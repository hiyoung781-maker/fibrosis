# 간경변F4_타겟발굴_REINVENT4_3주계획

# 간경변(MASH F4) 타겟 발굴 → REINVENT4 Lead 도출

## 3주 실행 계획 · 2인 · A100 ×3

---

## 0. 확정된 결정 사항

| 항목 | 결정 |
| --- | --- |
| 적응증 | **MASH 유래 대상성 간경변 (F4)** — 유지 |
| 주분석 대비 | **F4 vs F0** — 대조군 제외, 두 코호트 동일 정의 (2026-09-17 3차 개정, §11). 구 주분석 `F4 vs F0+대조`는 민감도 3으로 강등. F3–F4 병합은 민감도 2 |
| 코호트 통합 | **ComBat 병합 금지** → 코호트별 독립 DEG + 순위/방향성 일치 검증 |
| 내부 타당성 검증 | **양성·음성 대조 타겟 재발견 설계** (아래 §2가 핵심) |
| AI 모델 | **Geneformer**(omics) + **REINVENT4**(생성) — 2개만 |
| 킬 게이트 | **Day 2 종료 시점 druggability 테스트** — 실패 시 즉시 설계 변경 |
| GATE 1 결과 | **통과** (2026-09-16: 등급 B 295개, A–C 457개) — 후보 공간 295개를 **축소하지 않고 유지**. ⚠ 구 주분석(F4 vs F0+대조) 기준 결과이며 **신 주분석으로 재실행 필요** (§11 3차 개정) |
| §2 채점 기준 | **효과크기 기준**(순위 백분위 + ROC-AUC). 추론 통계는 보조 — 2026-09-17 개정, §11 참조 |
| §2 음성 대조군 라벨 | 단위별 **양식·실패 사유·결합 입증** 주석 → **strict / ambiguous 분리**. 헤드라인 = 계열 2(6단위) + strict 계열 1(3단위). `LOXL2`는 ambiguous — 2026-09-17 2차 개정, §11 참조 |
| 대조군과 최종 제안 | 대조군은 **채점용**이며 신규 제안에서 빠진다(순환논리 회피 + 신규성). **타겟 유효성 판단이 아니고, 순위에서 강등하지도 않는다** |
| wet lab | **수행하지 않는다.** 실험 검증 자리는 §2 채점·독립 코호트 재현·세포형 귀속·도킹으로 대체 |

---

## 1. 이 연구를 한 문장으로

> **“본 연구는 MASH 유래 대상성 간경변(F4) 의 미충족 의료 수요를 극복하기 위해, 기승인 및 실패된 임상 타겟을 벤치마크로 삼아 다층 오믹스 데이터와 파운데이션 모델로 유효 타겟을 발굴하고, 생성형 AI 를 통해 이를 제어할 새로운 소분자 선도 물질을 설계하는 연구이다.”**
> 

---

## 2. “임상 성적표로 파이프라인을 채점한다”

이것이 다른 학부 omics 발표와 갈리는 지점이고, 포스터 한가운데 들어갈 그림이다.

MASH/간섬유화는 **어떤 타겟이 성공했고 어떤 타겟이 실패했는지 이미 임상에서 판정이 났다.** 이것을 벤치마크로 쓴다.

대조군은 **약물 프로그램 단위**로 묶는다. 한 약이 여러 유전자를 동시에 노리면(pan-PPAR, cenicriviroc, emricasan) 그것은 독립적인 증거 1개다. 단위 점수는 그 단위에서 가장 순위가 높은 구성원으로 정하고, 양성과 음성에 같은 규칙을 적용한다.

**양성 대조군 — in-scope (소분자 등급 A–C 보유)**

| 단위 | 유전자 | 임상 근거 | 치료 방향 | 등급 |
| --- | --- | --- | --- | --- |
| THR-β | `THRB` | resmetirom 2024 FDA 가속승인 (F2–F3). MASLD 간에서 THR-β 신호 저하(“intrahepatic hypothyroidism”) | **활성화** | A |
| PPAR | `PPARA` `PPARD` `PPARG` | lanifibranor(pan-PPAR) 3상 | **활성화** | A |
| HSD17B13 | `HSD17B13` | INI-822(Inipharm) 소분자 저해제 1상 착수 | 억제 | B |

**음성 대조군 계열 1 — 임상 실패 (in-scope, 2026-09-17 2차 개정: strict / ambiguous 분리)**

임상 실패는 **프로그램 실패**이고 타겟 무효화가 아니다. 타겟에 대한 정보를 주는 것은 **“표적 결합이 입증됐는데 유효성이 없었다”** 한 경우뿐이며, 독성·PK·환자 선택·상업적 중단은 타겟에 대해 거의 아무 말도 하지 않는다. 따라서 각 단위에 **실패 약물의 양식 / 실패 사유 / 표적 결합 입증 여부** 3열을 붙이고, 세 조건을 모두 만족하는 단위만 **strict**로 헤드라인 채점에 쓴다.

- **strict 요건 3개**: ① 실패 약물이 **경구 소분자**(우리 산출물과 양식 일치) ② 실패 사유가 **유효성** ③ **표적 결합(target engagement) 입증**
- ②③이 미확인이거나 ①이 어긋나면 **ambiguous**로 분리해 따로 보고한다. **지우지 않는다.**

| 단위 | 유전자 | 실패 근거 | 양식 | 실패 사유 | 결합 입증 | 분류 | 등급 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ASK1 | `MAP3K5` | selonsertib STELLAR-4 실패 | 경구 소분자 ✓ | 유효성 | **미확인 — P1** | strict(잠정) | A |
| CCR | `CCR2` `CCR5` | cenicriviroc AURORA 무익성 중단 | 경구 소분자 ✓ | 유효성 | **미확인 — P1** | strict(잠정) | A |
| CASP | 검정된 9개 (`CASP1` `CASP2` `CASP3` `CASP4` `CASP6` `CASP7` `CASP8` `CASP9` `CASP10`) | emricasan 실패 | 경구 소분자 ✓ | 유효성 | **미확인 — P1** | strict(잠정) | A |
| **LOXL2** | `LOXL2` | simtuzumab 2b상 실패 (F3 219명 / 대상성 간경변 258명) | **인간화 IgG4 항체 ✗** | 유효성 | **의심** | **ambiguous** | A |

**LOXL2를 ambiguous로 내린 이유 (3겹)**

1. **양식 불일치** — §11 개정 6번은 `PNPLA3`·`SERPINH1`을 “임상 프로그램이 ASO/siRNA이므로 소분자 파이프라인을 이것으로 채점하면 산출물과 채점 기준이 어긋난다”는 이유로 out-of-scope로 뺐다. simtuzumab은 항체다. **같은 규칙을 음성에 적용하지 않으면 채점 기준이 비대칭이고, 심사에서 지적되면 채점 슬라이드 전체가 흔들린다.**
2. **표적 결합 의심** — simtuzumab의 epitope는 LOXL2의 SRCR4 도메인이고 촉매 활성 부위가 아니다. 항-LOXL2 항체(AB0023 계열)가 LOXL2 촉매 활성·콜라겐 가교·조직 경화를 저해하지 못한다는 보고가 있고, 이는 simtuzumab 무효성 해석에 직접 연결된다. 즉 “타겟이 틀렸다”의 근거로 **가장 약한 칸**이다.
3. **소분자로는 아직 실패하지 않았다** — 경구 범-LOX 저해제 **PXS-5505**가 골수섬유증 1/2a상 진행 중이고 CCl₄ 간섬유화 모델에서 효과를 보였다. LOXL2 선택적 소분자 **PAT-1251**도 담도 간섬유화에서 보고됐다. “LOXL2는 소분자로 실패한 타겟”이라는 문장은 성립하지 않는다.

**strict(잠정) 3단위는 P1이 결합 입증 여부를 채워야 확정된다.** 미확인으로 남으면 그 단위도 ambiguous로 내리고, 그 사실을 표에 그대로 공개한다. 근거 문헌은 단위당 1줄씩 붙인다(§6 Day 1).

**음성 대조군 계열 2 — 구조 단백질 (in-scope, 2026-09-17 추가)**

소분자로 제어하는 프로그램이 존재하지 않는데 F4에서 강하게 증가하므로, **발현 기반 순위는 이들을 무조건 위로 올린다.** 즉 “올라가면 파이프라인이 틀렸다는 것이 사전에 확실한” 유전자다. GATE 3·4가 이들을 강등시키는지가 “네트워크·세포형 근거가 fold change보다 낫다”는 우리 주장의 직접 시험이 된다.

| 단위 | 유전자 |
| --- | --- |
| COL1A1 | `COL1A1` |
| COL4 (α3α4α5 삼합체 = 1단위) | `COL4A3` `COL4A4` `COL4A5` |
| KRT7 | `KRT7` |
| TPM1 | `TPM1` |
| POSTN | `POSTN` |
| FLNC | `FLNC` |

6단위 8유전자. 전부 등급 B이고 근거는 `Structure with Ligand` 단독(`sm_evidence = weak`)이다. **주의**: “근거가 약한 칸에 ECM이 모여 있다”는 서사는 데이터로 성립하지 않는다 — `VIM`은 High-Quality Pocket을, `ITGA2`·`ITGB6`는 High-Quality Ligand를 갖고 있어 weak로 분리되지 않는다. 포스터에 그렇게 쓰지 말 것.

**Out-of-scope 패널 — 소분자 불가 (채점 대상 아님, 별도 보고)**

- `PNPLA3` — 인간 유전학으로 가장 강하게 검증된 위험 유전자. 임상 프로그램은 ASO(AZD2693). 소분자 등급 **없음**
- `SERPINH1` (HSP47) — BMS-986263은 지질나노입자 siRNA. 소분자 등급 **없음**

파이프라인이 이 둘을 소분자 후보로 올리지 않은 것은 결손이 아니라 **정답 출력**이다. 소분자를 산출물로 내놓는 파이프라인을 siRNA/ASO 타겟으로 채점하면 “당신들 결과물은 소분자인데 왜 siRNA 타겟으로 채점했나”에 답할 수 없다. 포스터에는 이렇게 쓴다: **“인간 유전학으로 가장 강하게 검증된 PNPLA3조차 우리 필터는 제외했다. 소분자로 건드릴 수 없기 때문이고, 실제 개발도 ASO로 진행 중이다.”**

**채점 방식 (2026-09-17 개정 — §11)**

1. **채점 범위**: 검정된 유전자 중 소분자 등급 A–C인 **5,007개**. DEG로 좁힌 457개가 아니다.
    - 이유: 양성 대조군 5개 중 주분석 DEG는 **0개**다(THRB 포함; 2026-09-16 구 주분석에서 확인, 2026-09-17 신 주분석 F4 vs F0에서도 동일). DEG 목록 위에서는 채점이 성립하지 않는다.
    - 생물학적 ****기전 ****확인: THRB, PPARA 같은 핵수용체나 대사 효소(HSD17B13)는 만성 질환 진행 시 mRNA 발현량 자체가 폭증하는 드라이버가 아니라, 단백질 활성 저하 및 정상 대사 부전(intrahepatic hypothyroidism 등)으로 질환에 기여한다. 따라서 단순 벌크 DEG 상위권에는 오르지 않으며, 전장 A–C 공간에서 네트워크 영향도(GATE 4)로 채점하는 것이 생물학적으로 유일하게 정당하다.
2. **대조군 처리**: 대조군(양성 3단위 + 음성 10단위 + out-of-scope 2개)은 **DEG 여부와 게이트 통과 여부에 무관하게** GATE 3·4를 끝까지 통과시킨다.
3. **방향**: 각 유전자를 치료 방향으로 채점한다. 대조군은 위 표의 방향을 쓰고, 미지 후보는 양방향 중 유리한 쪽을 쓴다. **대조군에도 동일 규칙을 적용한다.**
    - 이 규칙이 없으면 AUC는 파이프라인 성능이 아니라 방향 차이를 측정한다. 양성 3단위 중 2단위(THR-β, PPAR)가 활성화 타겟이고 음성은 전부 억제 타겟이기 때문이다.
4. **판정 기준 (효과크기)** — 2026-09-17 2차 개정: 헤드라인 음성 집합을 **계열 2 + strict 계열 1**로 한정
    - **헤드라인 집합**: 양성 3단위 vs 음성 9단위(**계열 2의 6단위 + strict 계열 1의 3단위**). ambiguous 단위(현재 LOXL2)는 헤드라인에서 제외한다.
    - 양성 단위 중위 순위 백분위 **≥ 75**
    - 음성 단위 중위 순위 백분위 **≤ 25**
    - ROC-AUC **≥ 0.75** (양성 3 × 음성 9 = 27쌍 → 0.037 간격)
    - **민감도 보고 2종을 함께 싣는다**: ① LOXL2 포함(음성 10단위, 30쌍, 0.033 간격) ② 계열 1만(양성 3 vs strict 3). 판정은 헤드라인 집합으로 하고 **세 숫자를 모두 공개한다** — 어느 집합을 골랐느냐로 결론이 바뀌는지가 그대로 보여야 한다.
    - **단위별 순위 백분위를 전부 표로 공개한다.** 계열 1과 계열 2는 실패 이유가 다르므로(타겟이 틀림 / 약으로 만들 수 없음) 따로 보고하고, 계열 1은 strict / ambiguous를 나눠 적는다.
    - **헤드라인의 무게는 계열 2에 있다.** 계열 2는 “타겟이 틀렸다”는 주장이 아니라 “소분자를 산출물로 내는 파이프라인의 오답”이라는 **정의**이고, 양식도 일치하며 6단위로 수가 더 많다. 계열 1보다 방법론적으로 깨끗하고, §3절의 라벨 오염 비판에 면역이다.
5. **추론 통계는 보조**: Mann–Whitney는 헤드라인 집합 또는 음성 두 계열 합산에서만 계산해 부기하고(strict 계열 1 단독으로는 계산하지 않는다 — 아래 검정력), “표본이 작아 추론 통계가 아니라 기술 통계로 보고한다”를 명시한다. 부트스트랩 신뢰구간은 쓰지 않는다 — 양성 3단위를 재표집한 구간은 해석할 수 없다.

> **사전 검정력 계산 (2026-09-17, 랭킹을 보기 전에 수행 / 2차 개정에서 갱신)**
프로그램 단위로 세면 순위가 완벽하게 갈려도 얻을 수 있는 최소 양측 p값은 `2 / C(양성+음성, 양성)`이다 — **헤드라인(양성 3 대 음성 9)에서 0.0091**, 합산(3 대 10)에서 0.0070, 개정 전 계열 1 전체(3 대 4)에서 0.0571, **strict 계열 1 단독(3 대 3)에서는 0.10**이다. 즉 개정 전 기준 `Mann–Whitney p<0.05`는 계열 1만으로는 **결과가 완벽해도 수학적으로 통과 불가능**했고, strict로 좁히면 더 불가능해진다. 이것이 판정을 효과크기로 옮긴 이유이며, **strict 계열 1을 단독 판정에 쓰지 않고 계열 2와 합쳐 헤드라인을 구성하는 이유**다(LOXL2를 ambiguous로 내린 대가는 27쌍 → 0.037 간격으로, 30쌍 0.033과 실질 차이가 없다). 또 “음성 백분위 ≤ 50”은 무작위 순위로도 0.62–0.69 확률로 충족되는 공짜 기준이므로 ≤ 25로 정했다.
> 

**대조군의 역할과 최종 제안 후보의 분리 (2026-09-17 2차 개정 — 명문화)**

개정 전에는 이 규칙이 “그 다음 순위의 **미지** 타겟이 우리의 제안이다” 한 구절에만 암시돼 있었다. 명문화한다.

1. **대조군(양성·음성 모두)은 채점용이며 신규 제안 후보에서 제외한다. 이것은 타겟 유효성 판단이 아니다.** 이유는 두 가지다.
    - **순환논리 회피** — 자(ruler)로 쓴 유전자를 결과물로 쓸 수 없다. `MAP3K5`를 음성 대조군으로 두고 동시에 최종 타겟으로 내면 채점 슬라이드가 자기모순이 된다(“우리 파이프라인은 ASK1을 아래로 내려서 검증됐고, 우리 제안은 ASK1입니다”).
    - **신규성 요건과 입증 부담** — 이미 임상에서 시도된 타겟은 신규 제안이 아니고, 3상 실패 타겟을 제안하면 §8 질의응답 5번을 7분 안에 이겨야 한다. **이 규칙은 양성 대조군에도 동일하게 적용된다**(`THRB`도 최종 제안에서 빠진다). 즉 제외 근거는 “나쁜 타겟이라서”가 아니다.
2. **음성 대조군을 순위에서 강등·삭제하지 않는다(하드 제외 금지).** 음성 단위가 상위에 오면 주석(양식·실패 사유·결합 입증)을 달아 **결과로 보고**한다. 해석은 사전에 이렇게 고정한다.
    - 그 결과는 **“파이프라인 오류”** 또는 **“실패가 타겟 수준이 아니라 프로그램 수준이었다”** 두 가설 중 하나이며, 후자를 주장하려면 별도 입증(F4 단계 특이성, 표적 결합, 환자 선택, 양식 전환)이 필요하다. **입증 없이 후자를 고르지 않는다.**
    - ambiguous 단위(현재 LOXL2)가 상위에 오는 것은 사전 기준상 **오류로 세지 않는다.** 헤드라인 AUC에서 이미 빠져 있다.
3. **채점이 측정하는 것을 과장하지 않는다.** 양성과 음성의 증거 수준이 애초에 다르다 — `HSD17B13`은 1상 착수로 **유효성 미입증**이고, `THRB`의 resmetirom 승인은 **F2–F3이며 F4는 라벨에서 제외**됐다. 반면 음성은 2b/3상 유효성 실패다. 따라서 이 채점은 “타겟의 진위”가 아니라 **“우리 순위가 현재 임상 프로그램의 관심 배분과 얼마나 일치하는가”**를 측정한다. 순위가 무작위가 아님을 보이는 유효한 검증이지만, 개정 전 문장 “파이프라인 자체가 검증된 것”은 데이터가 지지하는 것보다 센 표현이므로 **쓰지 않는다.**
4. **합의 재현과 발견은 방향이 반대다.** AUC 1.0은 “우리 점수가 업계 합의를 완벽히 복제한다”는 뜻이고, 연구의 가치는 합의가 놓친 것을 찾는 데 있다. 음성이 위로 오는 것은 **“합의가 맞다”는 가정 하에서만** 오류다. 이 긴장을 포스터 ‘한계’ 박스에 한 줄로 명시한다.

채점을 통과하면 **순위가 임상 성적표와 무작위 이상으로 일치한다는 것**이 보인 것이고, 그 다음 순위의 미지 타겟이 우리의 제안이다.

> 이이 슬라이드 한 장이 “연구 내용의 타당성” 심사 항목을 통째로 가져간다. 질의응답에서 “그 타겟이 진짜라는 근거가 뭐냐”는 질문에 대한  미리 준비되어 있는 셈이다.
> 

---

## 3. 데이터 확정

| 역할 | 데이터셋 | 구성 | 주의점 |
| --- | --- | --- | --- |
| **발굴 (bulk)** | GSE135251 | F4 14 / F3 54 / F2 53 / F1 47 / F0 38 / 대조 10 = 216 | NextSeq 500 **단일 플랫폼** (배치 리스크 낮음) |
| **독립 재현 (bulk)** | GSE162694 | F4 12 / F3 8 / F2 27 / F1 30 / F0 35 / 정상 31 = 143 | HiSeq 3000(GPL21290) **단일 플랫폼**, Salmon(hg19) 정수 count. GEO series matrix에 **배치 필드 없음** → `design = ~ group` (논문 보충자료의 배치 정보는 미확인) |
| **세포형 귀속 (scRNA)** | GSE136103 | 간경변 5 vs 건강 5 (CD45± 분획) | 기증자 5v5 → **정성적 근거**로만. 통계 주장 금지 |
| **세포형 귀속 (snRNA)** | GSE202379 | 47명, healthy → MASLD → MASH → **cirrhosis → end-stage**, ~10만 핵 | **Geneformer 기질.** 단계별 기증자 수는 미확인 → Day 1에 확인 |
| 외부 벤치마크 (비교) | GSE25097  | 간경변(LF) 40 vs 정상 6 (전체 511 샘플 중 서브셋 발라냄)  | 마이크로어레이(U133 Plus 2.0). 간암 절제 조직(바이러스/알코올성). 선행연구 NQO1 비교용 |

**주분석 대비 (2026-09-17 3차 개정, §11)**: GSE135251에서 `F4 (n=14)` vs `F0 (n=38; NAFL 33 + NASH_F0-F1 5, 대조군 제외)`

- 대조군을 기준군에 넣지 않는 이유: 기준군 안의 정상 간 비율이 코호트마다 달라진다(GSE135251 8/46 = 17%, GSE162694 31/66 = 47%). 비율이 다르면 두 코호트의 기준군이 서로 다른 집단이 되고, 정상 간 대비 대사질환 차이가 DEG에 코호트마다 다른 양으로 섞인다. F0만 쓰면 두 코호트 모두 “MASLD는 있으나 섬유화 0”이라는 같은 기준군이 된다.

**민감도 분석 1**: F4 vs F0–F1 (n=85, 대조군 제외)
**민감도 분석 2**: F3+F4 (n=68) vs F0–F1 (n=85)
**민감도 분석 3 (구 주분석)**: F4 (n=14) vs F0 + 대조 (n=46; 섬유화 단계 >0인 대조군 2명 GSM3998224·GSM3998341 제외)

**재현 (GSE162694, 같은 정의)**: 주 `F4 (n=12)` vs `F0 (n=35)` / 민감도 1 F4 vs F0–F1 (n=65) / 민감도 2 F3+F4 (n=20) vs F0–F1 (n=65) / 민감도 3 F4 vs F0 + 정상 (n=66) / 보조 F4 vs 정상 (n=31)

**추가 대비 (2026-09-17 사전등록 — §11)**

- **F4 vs F3** (14 vs 54): “F4는 F3에 없는 기전을 포함한다”는 §0의 결정을 데이터로 직접 검증하는 대비. 발표 질의응답 2번의 근거.
- **단계 추세** (NAFLD 206명, stage 0→4 연속형):
    - WGCNA(가중 유전자 공발현 네트워크)는 GATE 4(Geneformer)와 허브 추출 기능이 중복되고 일정 리스크가 크므로 전면 도입하지 않는다.
    - 대신 DESeq2에 스플라인 항을 넣고 선형 모델과 LRT(reduced=~1)로 비교하여 말기 급증(late surge) 패턴을 추출하고, 보조로 스피어만 순위 상관계수(\rho > 0.4, p < 0.05) 및 후기 급증비(\text{Mean}*{F3,F4} - \text{Mean}*{F0\text{-}F2} > 1.0)를 산출해 주석 컬럼(late_stage_surge)으로 추가한다.
    - 실험적 용량-반응 관계를 대신하는 근거이며, DEG가 아닌 유전자(THRB 등)에도 연속 점수를 주므로 §2 채점 범위 문제를 해결한다.
    - 대조군 10명은 stage 0에 섞지 않고 별도 기준점으로 둔다.
    - **사전 경고 2개**: ① 급증 지표는 F4 14명이 전부 결정하므로 패턴 분류의 부트스트랩 안정성을 함께 보고한다. ② 급증 패턴은 담도 반응 표지자(`SOX9` `CD24` `EPCAM` `KRT7`)로 오염된다 — GATE 3의 세포 구성 판정(아래)을 반드시 통과시킨다.

**실행 결과 (2026-09-17, 신 주분석)** — `scripts/01_deseq2_GSE135251.R`

| 대비 | case | ref | 검정 유전자 | padj<0.05 | DEG 증가 | DEG 감소 | DEG 합계 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **주분석** F4 vs F0 | 14 | 38 | 16,248 | 3,418 | 1,458 | 410 | **1,868** |
| 민감도 1 F4 vs F0–F1 | 14 | 85 | 16,947 | 4,151 | 1,650 | 407 | 2,057 |
| 민감도 2 F3+F4 vs F0–F1 | 68 | 85 | 15,268 | 4,367 | 756 | 118 | 874 |
| 민감도 3 F4 vs F0+대조 (구 주분석) | 14 | 46 | 16,492 | 2,969 | 1,315 | 422 | 1,737 |

- 민감도 3은 2026-09-16 구 주분석 산출물과 **파일 단위로 동일**하게 재현됐다.
- 신 주분석과의 일치도(공통 검정 유전자의 log2FC Spearman ρ / 양쪽 DEG 교집합 내 방향 일치): 민감도 1 ρ = 0.978(교집합 1,642개, 100%), 민감도 2 ρ = 0.912(756개, 100%), 민감도 3 ρ = 0.936(1,479개, 100%).
- 대조군 DEG 여부는 신·구 주분석에서 같다: 양성 5개 DEG 0개, 음성 계열 1은 `CCR2`·`LOXL2`만 DEG, 계열 2는 8개 전부 DEG.

**구 실행 결과 (2026-09-16, 구 주분석 F4 vs F0+대조)**: 주분석 DEG 1,737개(증가 1,315 / 감소 422), 민감도 1에서 2,057개, 민감도 2에서 874개. 주분석과 민감도 1의 log2FC Spearman ρ = 0.92(방향 일치 99.8%), 민감도 2와는 ρ = 0.81.

> n=14는 DESeq2에 충분하지만(Schurch 기준 12 근접), **분류기(AUC)를 만들기엔 양성 14개로 신뢰구간이 ±0.15 수준**이다. 따라서 머신러닝 분류기는 주 결과가 아니라 보조 패널로만 넣는다(§4-C). GPU와 시간은 Geneformer에 쓴다.
> 

---

## 4. 파이프라인 — 5개 게이트

각 게이트는 통과/탈락 기준이 **사전에 숫자로** 정해져 있다. 포스터에 “우리는 시작 전에 이 기준을 정해뒀다”고 쓸 수 있는 것 자체가 점수다.

### GATE 1 — Druggable universe 선-필터 (Day 1–2, 킬 게이트)

전체 유전자에서 시작하지 않는다. **“소분자로 건드릴 수 있다고 이미 평가된 유전자”만** 후보 공간으로 둔다.

- **이유**: 소분자로 개발할 수 없는 유전자를 후보 공간에서 먼저 뺀다. 분비 케모카인·면역글로불린·대부분의 ECM 단백질이 여기서 제외된다.
- **정정 (2026-09-17)**: 개정 전 이 칸에는 “ECM 구조 단백질 지배 문제를 랭킹 이전에 제거한다”고 적혀 있었다. **실행 결과 이 주장은 성립하지 않는다.** `COL1A1` `COL4A3/4/5` `KRT7` `TPM1` `POSTN` `FLNC` `VIM`이 모두 등급 B로 살아남았다. `Structure with Ligand`는 “소분자와 공결정된 PDB 구조가 하나라도 있다”는 뜻일 뿐 약물유사성·효능·scaffold 요건이 없기 때문이다. 따라서 **ECM 방어선은 GATE 1이 아니라 GATE 3(세포형 귀속)과 GATE 4(네트워크 영향도)가 담당한다.** 살아남은 구조 단백질 8개는 지우지 않고 §2 음성 대조군 계열 2로 정식 편입해, GATE 3·4가 이들을 강등시키는지를 검증 지표로 쓴다.

#### 무엇을 쓰는가 — Open Targets *tractability*

Open Targets Platform이 모든 인간 단백질에 대해 미리 계산해 둔 “약으로 만들 수 있는 정도” 평가다. 소분자(SM)·항체(AB)·PROTAC(PR)·기타 임상(OC) 네 가지 양식(modality)을 따로 평가하며, 우리는 **소분자(SM)만** 본다.

소분자 카테고리 (위에서부터 근거가 강함):

| 등급 | 카테고리 | 뜻 |
| --- | --- | --- |
| **A**임상 선례 | `Approved Drug` | 이 타겟에 대한 **4상/승인** 약이 존재 |
|  | `Advanced Clinical` | 2·3상 약이 존재 |
|  | `Phase 1 Clinical` | 1상 약이 존재 |
| **B**★ 우리 사냥터 | `Structure with Ligand` | 리간드와 **공결정 구조**가 있음 |
|  | `High-Quality Ligand` | 양질의 리간드가 확인됨 |
|  | `High-Quality Pocket` | DrugEBIlity 포켓 점수 ≥ 0.7 |
| **C**약한 근거 | `Med-Quality Pocket` | DrugEBIlity 0–0.7 |
|  | `Druggable Family` | Finan et al. druggable genome 기준 충족 (GPCR·키나아제·이온채널·핵수용체·PDE 등) |

**왜 B가 사냥터인가**: 등급 A는 이미 약이 있으니 신규성이 없다 — 대신 **§2 양성 대조군 채점에 딱 맞는 칸**이다. 등급 B는 *리간드나 포켓은 확인됐는데 아직 임상 선례가 없는* 구간, 즉 **실행 가능성과 신규성이 동시에 성립하는 유일한 칸**이다. 이 논리 한 줄이 “왜 하필 그 타겟이냐”에 대한 답이 된다.

**교차 확인용 두 번째 기준** — Finan et al. 2017 *Sci Transl Med* 「druggable genome」:

- **Tier 1 (1,427개)**: 승인약·임상단계 약물의 효능 타겟, 또는 IC50/EC50 ≤ 100 nM 화합물이 있는 단백질
- **Tier 2 (682개)**: 승인 타겟과 서열 동일성 ≥ 50%, 또는 ChEMBL에 ≤ 1 µM drug-like 화합물이 있는 단백질
- **Tier 3 (2,370개)**: 분비·세포외 단백질, 원거리 상동체(≥25%), 주요 druggable 계열

Open Targets의 `Druggable Family`가 이 Tier 기준을 그대로 쓰므로 둘은 독립적이지 않다. **Finan Tier 1–2는 주 기준이 아니라 sanity check로만** 쓴다 (두 리스트가 크게 어긋나면 파싱 오류를 의심).

링크

- Open Targets tractability 문서 — https://platform-docs.opentargets.org/target/tractability
- Open Targets Platform (유전자 페이지에서 직접 확인) — https://platform.opentargets.org/
- 전체 데이터 다운로드 (Parquet, `target` 데이터셋의 `tractability` 필드) — https://platform.opentargets.org/downloads
- GraphQL API (프로그램으로 뽑을 때) — https://api.platform.opentargets.org/api/v4/graphql/browser
- 방법론 원논문: Brown et al. 2018, *Approaches to target tractability assessment*
- Finan et al. 2017, *Sci Transl Med* — https://www.science.org/doi/10.1126/scitranslmed.aag1166 (무료 전문: https://www.ncbi.nlm.nih.gov/pmc/articles/PMC6321762/)

#### 판정 기준

- **통과**: F4 vs F0 DEG(FDR<0.05, |log2FC|>0.5) 중 **등급 B에 해당하는 유전자 ≥ 10개**, 그리고 등급 A–C 전체로는 ≥ 20개
    - $|\log_2\text{FC}| > 0.5$  채택의 생물학적 근거: 암 조직과 달리 만성 간섬유화 조직에서 질환 스위치를 담당하는 핵심 효소, 키나아제, 수용체는 1.4~1.6배`$(\log_2\text{FC} \approx 0.5\sim0.7)$`만 변해도 강력한 신호 증폭을 유도한다. 무리하게 1.0(2배)으로 올리면 콜라겐 같은 잔해 단백질만 남고 정작 약물화 가능한 상류 조절자가 모조리 탈락하므로, 표준 만성질환 스크리닝 기준(0.5)을 사전 고수한다.
- **탈락 시 1차 대응**: 등급 C까지 후보 공간 확장
- **그래도 미달이면**: 타겟을 단백질이 아니라 **전사인자 활성/신호 경로** 수준에서 정의하고, 그 경로의 상류 효소를 공략 (GATE 3의 decoupleR 결과 활용)
- Day 2 안에 반드시 결론을 낸다. 이 게이트가 REINVENT4까지 갈 수 있는지를 결정하는 유일한 변수다.

#### 판정 결과 (2026-09-16) — **통과**

> ⚠ 아래 숫자는 **구 주분석(F4 vs F0+대조, 현 민감도 3)** 기준이다. 주분석이 F4 vs F0로 바뀌었으므로(§11 3차 개정) 신 주분석 DEG로 GATE 1을 재실행해 이 표를 갱신한다. 판정 기준(B ≥ 10, A–C ≥ 20)은 바꾸지 않는다.

| 등급 | 증가 | 감소 | 합계 |
| --- | --- | --- | --- |
| A (임상 선례 있음) | 75 | 22 | 97 |
| B (리간드·포켓 있음, 임상 선례 없음) | 213 | 82 | **295** |
| C (약한 근거) | 49 | 16 | 65 |
| 없음 | 978 | 302 | 1,280 |

기준(B ≥ 10, A–C ≥ 20)을 크게 넘겼으므로 등급 C 확장이나 경로 기반 전환은 발동하지 않는다. 등급 A–C 합계 457개.

**후보 공간은 295개를 그대로 유지하고 축소하지 않는다** (2026-09-17 결정, §11). 대신 순위 계산에 쓰지 않는 주석 열을 붙인다.

- `sm_evidence = strong` — High-Quality Ligand 또는 High-Quality Pocket 보유 (**160개**)
- `sm_evidence = weak` — 그 외 (`Structure with Ligand` 단독 114개 + `Structure with Ligand`에 Med-Quality Pocket/Druggable Family만 더해진 21개 = **135개**)
- 용도는 두 가지뿐이다: ① 동점 처리 시 strong 우선 ② GATE 5 진입 판단. **순위 계산에는 절대 쓰지 않는다.**

산출물: `results/gate1/GSE135251_main_tractability.tsv` (검정 유전자 전체 + 등급), 등급 A·B DEG 목록. 실행 스크립트 `scripts/02_gate1_tractability.py`.

### GATE 2 — 독립 재현 (Day 3–5)

GSE135251 DEG와 GSE162694 DEG를 **각각 독립적으로** 계산하고 순위만 비교한다. 비교 대비는 두 코호트 모두 **주분석 F4 vs F0**이다(§3).

- 지표: 발굴 상위 500 DEG의 방향성 일치율, Spearman ρ, RRHO(rank-rank hypergeometric overlap)
- **통과 기준**: 방향성 일치율 **≥ 70%** (이항검정 vs 50%, p < 1e-10) **그리고** Spearman ρ ≥ 0.3
- **탈락 시**: F4 정의 재확인(대상성/비대상성 혼재 여부) → 그래도 실패면 민감도 분석(F3F4)으로 주분석 전환

### GATE 3 — 세포형 귀속 (Day 6–8)

- GSE202379: 기증자 단위 pseudobulk → DESeq2 (cirrhosis/end-stage vs healthy), 세포형별
- GSE136103: 동일 방향성 확인 (정성)
- `decoupleR` + CollecTRI로 **전사인자 활성** 추정 → 구조 단백질이 아닌 **신호 노드**를 표면화
- (여유 시) `LIANA`로 대식세포↔︎HSC 리간드-수용체 축
- **통과 기준**: 후보가 HSC / scar-associated macrophage / 담관세포 / 간세포 중 **최소 1개 세포형에서 기증자 단위로 유의** (FDR<0.1)

**세포 구성 판정 (2026-09-17 추가 — 최대 잔여 위험)**

벌크 상위 DEG에 담도 반응 표지자가 몰려 있다: `SOX9`(+2.17) `CD24`(+2.04) `EPCAM`(+2.12) `KRT7`(+2.73). **세포가 늘어난 것**과 **세포 안에서 발현이 올라간 것**을 구분하지 못하면 이런 표지자가 상위를 차지한다. 선행연구가 EPCAM(논문 1)과 SOX9·CD24(논문 3)를 후보로 뽑은 것도 같은 현상으로 보인다.

- **사전 기준**: 후보는 해당 세포형 **내부의** 기증자 단위 pseudobulk 대비에서 유의해야 한다(FDR<0.1). 세포형 비율 변화만으로 설명되는 유전자(세포형 내부에서 유의하지 않고 비율 변화와만 상관)는 **후보에서 강등**하고, 강등 사실을 표로 공개한다.
- ECM 지배보다 이쪽이 더 큰 위험이다. GATE 1이 ECM을 걸러주지 못한다는 것이 확인됐으므로(위 GATE 1 정정), 구조 단백질 강등도 이 판정이 담당한다.

### GATE 4 — Geneformer 재순위 (Day 9–12)

§5 참조. 출력은 후보별 **cell-state shift score**.

- **통과 기준 (2026-09-17 개정, 같은 날 2차 개정)**: §2의 효과크기 기준 — **헤드라인 집합(양성 3단위 vs 계열 2의 6단위 + strict 계열 1의 3단위)**에서 양성 단위 중위 순위 백분위 ≥ 75, 음성 ≤ 25, ROC-AUC ≥ 0.75(27쌍, 0.037 간격). 단위별 백분위 표를 strict / ambiguous로 나눠 공개하고, 민감도 2종(LOXL2 포함 / 계열 1만)을 함께 싣는다. Mann–Whitney는 보조 부기로만.
    - **ambiguous 단위(LOXL2)가 상위에 와도 탈락으로 세지 않는다.** 강등하지 않고 주석 달아 보고한다(§2 대조군 역할 규칙 2번).
    - 개정 전 기준 `Mann–Whitney p<0.05`는 독립 프로그램 단위가 적어 결과가 완벽해도 통과 불가능했다(§2 사전 검정력 계산).
    - **대조군은 게이트 통과 여부와 무관하게 전량 섭동을 돌린다.** 양성 대조군 5개는 F4 vs F0 DEG가 아니므로, 이 규칙이 없으면 GATE 1에서 탈락해 GATE 4에 도달하지 못하고 채점 자체가 불가능해진다.
- **탈락 시**: Geneformer 층을 결과에서 제외하고 “우리는 이 방법을 시도했고 대조군 검증에 실패해서 채택하지 않았다”를 **정직하게 슬라이드로 넣는다.** 이건 감점이 아니라 오히려 방법론적 성숙함으로 읽힌다. (사후 논점 전환이 아니라 사전 기준에 따른 기각이므로 방어 가능)

### GATE 5 — 화학 진입 조건 (Day 13)

최종 타겟 1개를 확정하기 전, 상위 후보 **3개를 병렬로** 다음을 확인:

- ChEMBL에 pChEMBL 값이 있는 활성 화합물 **≥ 300개** (ChemProp 학습 최소선)
- 단백질 클래스가 소분자 친화적 (GPCR / 키나아제 / 핵수용체 / 효소 / 이온채널)
- **소분자 등급에 `High-Quality Ligand` 포함 (2026-09-17 추가)**
    - 근거는 결과가 아니라 정의다. High-Quality Ligand는 ChEMBL 리간드에 PFI·SMART hit·scaffold 요건을 걸어 산출되므로 ChEMBL 데이터 존재가 사실상 보장된다. `Structure with Ligand` 단독 유전자는 ChEMBL 화합물이 있다는 보장이 전혀 없어 Day 13에 ChEMBL 부족으로 타겟이 죽는 사고가 난다.
    - **`sm_evidence = strong`으로는 불충분하다**: strong 160개 중 39개는 High-Quality Pocket만 보유하며, 이는 구조 기반 점수라 ChEMBL 화합물을 보장하지 않는다. 등급 B 중 High-Quality Ligand 보유는 121개.
    - weak 유전자가 최종 top 3에 들어오면 **결과로는 보고하되 REINVENT4에는 보내지 않는다.**
- 선행 문헌 존재 (P1이 문헌으로 검증). **본 연구는 wet lab을 수행하지 않는다** — 문헌 근거 확인까지가 범위이고, 실험은 §7의 “제안하는 검증 실험”으로만 제시한다
- **300개 미만이면**: Mol2Mol 모드로 전환(알려진 tool compound 1개 최적화) 또는 2순위 타겟으로 이동

---

## 5. AI 모델 스택 — Q2 최종 답

### A. Omics: **Geneformer** (Hugging Face, Apache-2.0, 게이팅 없음)

**왜 이것인가**

1. 2026년 *Nature Protocols*에 「Discovery of candidate therapeutic targets with Geneformer」라는 **단계별 공식 프로토콜**이 나와 있다. 3주짜리 학부 프로젝트에서 “출판된 프로토콜을 그대로 실행했다”는 것은 재현성·타당성 방어에 결정적이다.
2. 프로토콜이 명시한 소요 시간이 **“2일 이내, 표준 GPU 워크스테이션”** — A100 3장이면 여유롭다.
3. **fold change가 아니라 네트워크 영향도로 순위를 매긴다.** 이것이 ECM 지배 문제의 세 번째 방어선이다. COL1A1은 발현 변화는 크지만 in silico 조작 시 세포 상태를 되돌리지 못한다.
4. 사전학습 가중치를 받아 fine-tune만 한다.

**실행 3단계**

1. **Zero-shot**: GSE202379를 토크나이즈(rank-value encoding) → 사전학습 임베딩에서 cirrhosis/healthy가 분리되는지 확인. 분리 안 되면 fine-tune으로 가도 소용없다는 조기 신호.
2. **Fine-tune**: 세포상태 분류기(cirrhosis vs healthy), 세포형별. **split은 반드시 기증자 단위** — `GroupKFold(groups=donor_id)`, 코드에 `assert` 로 강제. 보고 지표: macro F1, confusion matrix.
3. **In silico perturbation**: GATE 1–3을 통과한 후보 유전자 전체 **그리고 §2 대조군 전체(게이트 통과 여부 무관)** 에 대해 **deletion**과 **activation** 양방향 시뮬레이션 → 임베딩이 healthy 쪽으로 이동하는 cosine shift를 점수화 → 재순위.
    - 방향 주의: `THRB`는 **활성화**가 치료 방향(작용제), 대부분의 profibrotic 타겟은 **억제**가 치료 방향. 후보별로 두 방향 다 돌리고 유리한 쪽을 보고하되, **대조군에도 같은 규칙을 적용**해야 공정하다.

**반드시 말해야 할 한계 (포스터 ‘한계’ 박스에 한 줄)**
> 단일세포 foundation model의 in-silico perturbation 성능이 단순 베이스라인(PCA 등)을 확실히 넘지 못한다는 벤치마크가 보고되어 있다. 따라서 Geneformer는 **단독 근거가 아니라 GATE 1–3을 이미 통과한 후보의 재순위 층**으로만 사용했고, 양성·음성 대조군으로 사전 채점했다.

이 한 문장이 있으면 “요즘 유행하는 모델 그냥 갖다 쓴 거 아니냐”는 질문이 원천 차단된다.

**GSE136103(5v5)로는 fine-tuning 하지 않는다.** 기증자 10명으로는 기증자 단위 split이 성립하지 않는다.

### B. 생성: **REINVENT4** (Apache-2.0)

**구조 예측이 필요 없는 이유**
REINVENT4의 스코어링 컴포넌트에 **ChemProp(v2, D-MPNN)이 내장**되어 있다. ChEMBL 활성 데이터로 ChemProp 회귀 모델을 학습해 리간드 기반으로 점수를 매기면 **점수 산출에는 단백질 구조가 전혀 필요 없다.** GATE 5에서 이미 “활성 화합물 ≥300개”를 요구하므로 학습 데이터도 보장된다. AlphaFold/Boltz는 선택 사항으로 남긴다.

**도킹은 선택 → 필수로 승격 (2026-09-17, §11)**

wet lab을 수행하지 않으므로, 생성 분자가 타겟 포켓에 실제로 들어갈 수 있는지 보여주는 수단이 도킹밖에 없다. 최종 대표 3개에 대해 기존 PDB 구조로 도킹을 돌리고 결합 자세를 포스터에 싣는다.

- 후보 대부분이 등급 B이고 그 근거가 `Structure with Ligand`(= 리간드 공결정 구조 존재)이므로 **도킹 대상 구조는 이미 확보되어 있다.** GATE 5 진입 요건인 High-Quality Ligand 보유 유전자는 더욱 그렇다.
- **한계를 같이 적는다**: 도킹 점수는 실제 결합력과 상관이 약하다. “결합 가능한 자세가 존재한다”는 주장까지만 하고 친화도 예측으로 쓰지 않는다. 친화도는 ChemProp 예측값으로 보고한다.

**설정**

- 생성기: `Reinvent` (de novo, RNN). 활성 화합물이 적거나 특정 계열을 발전시키고 싶으면 `Mol2Mol`
- 실행 모드: `staged learning` (2단계 커리큘럼)
    - **Stage 1** — 물성·필터만: QED, SA score, MW 250–500, logP 1–4, TPSA 40–120, PAINS/SMARTS alert. 화학공간을 먼저 정리
    - **Stage 2** — ChemProp 예측 pIC50을 높은 가중치로 추가
- 다양성 필터: `IdenticalMurckoScaffold`, bucket 25 (mode collapse 방지)
- **표절 방지 검증**: 생성 분자의 ChEMBL 학습셋 대비 **최대 Tanimoto 유사도 분포**를 반드시 보고. 0.85 이상은 “새 분자가 아님”으로 배제. 이걸 안 하면 질의응답에서 무너진다.
- 최종 산출: 상위 10개 → Murcko scaffold 기준 중복 제거 → **대표 3개를 포스터에 구조식으로**

### C. 쓰지만 크게 내세우지 않을 것

- `DESeq2` / `limma` / `decoupleR` / `Scanpy`
- 분류기(elastic net, n=52/78)는 **보조 패널 한 칸**. AUC와 신뢰구간을 같이 적어 과장하지 않는다.

---

## 6. 3주 일정 (21일)

**역할**: **P1 = 간 전공 팀원** — 생물학 해석, 대조군 리스트 검증, 문헌, 발표·질의응답, 포스터 서사. **P2 = 본인** — 코드, GPU, 모델 실행.

### Week 1 — 근거 확보

| Day | P1 | P2 | GPU |
| --- | --- | --- | --- |
| 1 | 양성/음성 대조군 리스트 확정 + 근거 문헌 1줄씩. **음성 단위마다 주석 3열(실패 약물 양식 / 실패 사유 / 표적 결합 입증 여부)을 채워 strict·ambiguous 확정** — 미확인은 ambiguous로 내리고 표에 공개 | GEO 메타데이터 수집, GSE202379 단계별 기증자 수 확인, 환경 구축 | — |
| 2 | resmetirom F4 제외 근거·임상 서사 자료 수집 | **GATE 1**: F4 vs F0 DEG → Open Targets tractability(SM) 등급 교차 → 통과/탈락 판정 | — |
| 3 | 상위 DEG 생물학적 타당성 1차 스크리닝 | ~~DESeq2 정식 실행~~ **Day 2에 완료** (주분석 + 민감도 2종) → 추가 대비 실행: **F4 vs F3**, **단계 추세**(스플라인 LRT) | — |
| 4 | 〃 | GSE162694 독립 DEG (`~group` — GEO 메타에 배치 필드 없음, §3) | — |
| 5 | 중간 리뷰: 말이 되는 후보인가 | **GATE 2** 판정 (일치율·ρ·RRHO 그림) | — |
| 6–7 | scRNA 세포형 해석 | GSE202379/GSE136103 전처리, 기증자 pseudobulk | #2 |

**Day 5 종료 시점 체크포인트**: GATE 1·2 통과 여부. 하나라도 실패면 주말에 설계 조정.

### Week 2 — 모델

| Day | P1 | P2 | GPU |
| --- | --- | --- | --- |
| 8 | decoupleR TF 결과 해석 | **GATE 3** 판정, TF 활성 + (여유 시) LIANA | #2 |
| 9 | 포스터 서사 초안 쓰기 시작 | Geneformer 토크나이즈 + zero-shot 평가 | #1 |
| 10 | 〃 | Geneformer fine-tune (기증자 단위 split, F1 보고) | #1, #2 |
| 11 | 대조군 결과 해석 | In silico perturbation (후보 + 대조군 전체, 양방향) | #1, #2 |
| 12 | 〃 | **GATE 4** 판정: 단위별 순위 백분위 표 + AUC (효과크기 기준, §2) | — |
| 13 | 최종 타겟 3개 **문헌** 근거 검증 (실험은 수행하지 않음) | **GATE 5**: ChEMBL 수집, 데이터 수 확인, High-Quality Ligand 보유 확인 | #3 |
| 14 | 타겟 1개 확정 (P1이 최종 발언권) | ChemProp v2 학습 + 검증 (scaffold split, RMSE/R²) | #3 |

**Day 14 종료 시점**: 타겟 확정. 이 시점에 타겟이 없으면 REINVENT4를 포기하고 “타겟 발굴까지”로 범위를 줄인다 — 이것도 완결된 발표가 된다.

### Week 3 — 화학 + 산출물

| Day | P1 | P2 | GPU |
| --- | --- | --- | --- |
| 15 | 포스터 레이아웃 초안 (A0) | REINVENT4 Stage 1 RL | #3 |
| 16 | 그림 초안 (파이프라인 도식) | REINVENT4 Stage 2 RL, 결과 수집 |  |
| 생성물 분석: Tanimoto·물성 분포·scaffold 신규성, 대표 3개 선정 | #3 |  |  |
| 17 | 포스터 v1 완성 | 모든 그림 최종본 렌더링 |  |
| 재현 스크립트 정리 (QR 코드로 GitHub 연결) | — |  |  |

> **Day 17의 QR 코드**는 TIP_2(“스스로 직접 해본 결과”)에 대한 가장 값싼 증명이다. 포스터 구석에 코드 저장소 QR 하나 넣는 데 30분이면 된다.
> 

---

## 7. 7분 발표 구성

| 시간 | 내용 | 슬라이드/포스터 영역 |
| --- | --- | --- |
| 0:00–1:00 | **훅**: 항섬유화 약물의 무덤 — selonsertib, simtuzumab, cenicriviroc, emricasan, OCA. 그리고 2024년 resmetirom 승인. **단, F4는 라벨에서 제외.** | 좌상단, 타임라인 그림 |
| 1:00–1:40 | **문제 정의**: 실패가 **타겟 수준**이었나 **프로그램 수준**(화합물·양식·환자 선택)이었나 — 이 둘을 구분하는 것이 우리 질문이다. 음성 대조군 주석 3열이 그 구분을 담는다 | 좌단 |
| 1:40–3:00 | **방법**: 3층 근거(벌크 발굴 → 독립 재현 → 세포형 귀속) + Geneformer 재순위. **“우리는 druggable 유전자만 처음부터 봤다”**를 반드시 말할 것 | 중앙 상단, 파이프라인 도식 |
| 3:00–4:20 | **★ 채점 슬라이드**: 양성 대조군은 위로, 음성 대조군은 아래로 정렬되었다 | **중앙, 포스터에서 가장 큰 그림** |
| 4:20–5:20 | **결과**: 제안 타겟 + 세포형 귀속 근거 | 우상단 |
| 5:20–6:20 | **REINVENT4**: 생성 분자 3개 구조식 + 물성 분포 + 신규성(Tanimoto) | 우중단 |
| 6:20–7:00 | **한계와 다음 단계**: wet lab 미수행을 먼저 밝히고 무엇으로 대체했는지 4개 제시, Geneformer 한계 명시, **음성 대조군 라벨의 한계 한 줄**(“임상 실패는 프로그램 실패다. 그래서 양식·사유·결합으로 걸러 strict만 채점했고, 합의를 완벽히 재현하는 파이프라인은 아무것도 발견하지 못한다”), 다음 단계로 **제안하는** 검증 실험 1개 | 우하단 |

**A0 포스터 3열 구성**

- **좌**: 문제(약물 무덤 타임라인) → 질문
- **중**: 방법 도식 → **채점 결과(가장 큰 그림)**
- **우**: 타겟 → 분자 → 한계/다음 단계
- 하단 띠: 사전 등록 기준 2개 + 데이터셋 표 + QR

---

## 8. 예상 질의응답 (P1이 준비)

1. **“F4가 14명인데 통계적으로 괜찮나?”** → Schurch 기준 그룹당 6명 이상, 12명이면 작은 변화까지. 14 vs 38이고, 독립 코호트 12 vs 35에서 같은 대비로 재현을 검증한다(GATE 2). 분류기는 신뢰구간이 넓어 보조로만.
2. **“왜 F3–F4를 합치지 않았나?”** → F4는 재생결절·문맥압항진 등 F3에 없는 기전을 포함. 합치면 F3 신호로 결론 내릴 위험. 민감도 분석으로 별도 제시. **데이터로도 확인됨 (2026-09-16, 구 주분석 F4 vs F0+대조 기준 — 신 주분석 GATE 1 재실행 후 갱신)**: F3+F4로 병합하면 DEG가 1,737 → 874개, 등급 A–C 후보가 457 → 261개로 **절반으로 줄어든다.** FDR 통과 유전자는 오히려 늘지만(2,969 → 4,367) F3가 효과크기를 희석해 |log2FC|>0.5 기준을 넘지 못한다. 즉 병합은 후보를 넉넉하게 만들지 않고 F4 신호를 지운다.
3. **“두 코호트를 왜 합치지 않았나?”** → 배치 보정이 생물학적 신호까지 깎을 수 있어, 순위·방향성 일치로만 비교. 더 보수적인 검증.
4. **“Geneformer 결과를 믿을 수 있나?”** → 못 믿을 수 있다. 그래서 대조군으로 사전 채점했고, 단독 근거가 아니라 재순위 층으로만 썼다. (벤치마크 한계도 인지)
5. **“왜 이 타겟이 실패한 타겟들과 다른가?”** → P1이 생물학으로 답해야 하는 유일한 질문. **Day 13–14에 답을 미리 만들어 둘 것.**
6. **“생성한 분자가 기존 화합물 베낀 것 아닌가?”** → 최대 Tanimoto 분포 제시, 0.85 이상 배제.
7. **“왜 항체가 아니라 소분자인가?”** → resmetirom·lanifibranor·denifanstat 모두 경구 소분자. F2–F3까지는 경구 소분자로 왔고 F4에서 멈춘 것.
8. **“wet lab 검증은 어떻게 할 건가?”** → **하지 않았다**고 먼저 말한다. 그 다음 실험을 무엇으로 대체했는지 4개를 제시한다: ① 독립 코호트 재현(GATE 2) ② 세포형 내재적 변화(GATE 3) ③ 임상 성적표 채점(§2) ④ 생성 분자 도킹. 그리고 **다음 단계로 제안하는 실험 1개**를 구체적으로 준비 (예: LX-2 또는 1차 HSC에서 TGF-β 자극 후 후보 억제 시 COL1A1/ACTA2 변화)
9. **“가장 유명한 타겟인 PNPLA3가 왜 결과에 없나?”** → 소분자로 건드릴 수 없어서 우리 필터가 제외했다. 실제 개발도 ASO(AZD2693)로 진행 중이다. HSP47(SERPINH1)도 같은 이유로 제외됐고 BMS-986263은 siRNA다. **소분자를 산출물로 내놓는 파이프라인이 이 둘을 올렸다면 그게 오류다.** (§2 out-of-scope 패널)
10. **“왜 통계 검정 대신 백분위와 AUC를 쓰나?”** → 독립적인 약물 프로그램 단위가 양성 3개뿐이라, 순위가 완벽하게 갈려도 Mann–Whitney 최소 양측 p값이 0.057이다. **랭킹을 보기 전에 이 계산을 하고** 기준을 효과크기로 바꿨고, 변경 일시와 이유를 §11에 기록했다. 검정은 보조로 부기한다.
11. **“임상에서 실패한 타겟이 상위에 올라오면 파이프라인이 틀린 건가? 실패는 약물 탓일 수도 있지 않나?”** → **맞는 지적이고, 그래서 사전에 처리 규칙을 만들어 뒀다.** 임상 실패는 프로그램 실패이고 타겟 무효화가 아니다. 타겟에 대한 정보를 주는 것은 “결합이 입증됐는데 유효성이 없었다” 한 경우뿐이다. 그래서 음성 단위마다 **양식·실패 사유·결합 입증** 3열을 붙이고 strict만 헤드라인 채점에 썼다. `LOXL2`는 실패 약물 simtuzumab이 **항체**이고(우리 산출물은 소분자), epitope가 촉매 부위가 아니며, 경구 범-LOX 소분자(PXS-5505)는 아직 실패하지 않았으므로 **ambiguous로 내려 헤드라인에서 뺐다.** 우리가 `PNPLA3`를 ASO라서 뺀 것과 **같은 규칙을 음성에도 적용한 것**이다. 그리고 음성이 상위에 와도 강등하지 않고 주석 달아 결과로 보고한다 — 다만 “실패는 타겟 탓이 아니었다”를 주장하려면 F4 단계 특이성·결합·환자 선택으로 별도 입증해야 하고, 입증 없이는 그 해석을 고르지 않는다.
12. **“양성 대조군이 DEG 목록에 없는데 어떻게 채점했나?”** → 채점은 DEG 목록이 아니라 소분자 등급 A–C 유전자 5,007개 전체 순위에서 한다. 대조군은 DEG 여부·게이트 통과 여부와 무관하게 모든 단계를 통과시킨다. THRB가 F4에서 발현이 **감소**해 있다는 것 자체가 작용제 개발(resmetirom)의 근거와 일치한다..

---

## 9. 남은 위험과 대응

| 위험 | 신호 | 대응 |
| --- | --- | --- |
| ~~ECM 유전자 지배~~ (해소: GATE 1 통과, B=295) | — | — |
| **ECM이 GATE 1을 통과함** (2026-09-17 신규) | 구조 단백질 8개가 등급 B에 잔존 | 지우지 않고 §2 음성 대조군 계열 2로 편입 → GATE 3·4가 강등시키는지로 검증. 강등 실패 시 파이프라인 한계로 정직하게 보고 |
| **담도 반응 표지자 지배** (2026-09-17 신규, 최대 잔여 위험) | 상위 DEG에 SOX9·CD24·EPCAM·KRT7 집중 | GATE 3 세포 구성 판정으로 강등. 세포형 내부에서 유의하지 않으면 후보 제외 |
| 재현 실패 | 방향성 일치율 < 70% | F4 정의(대상성/비대상성) 확인 → 민감도 분석으로 주분석 전환 |
| GSE202379 단계별 기증자 부족 | Day 1 확인 결과 cirrhosis 기증자 < 5 | Geneformer를 MASH vs healthy로 재정의 (F4 전용 주장 포기) |
| 대조군 채점 실패 | GATE 4에서 효과크기 기준 미달 (양성 중위 백분위 < 75 또는 AUC < 0.75) | Geneformer 층 제외, 정직하게 슬라이드화 |
| **음성 대조군 라벨 오염** (2026-09-17 2차 개정 신규) | 실패 사유가 유효성이 아님(독성·PK·환자 선택) 또는 양식 불일치(simtuzumab은 항체) → “실패 = 타겟이 틀림”이 성립하지 않음 | 단위별 양식·사유·결합 입증 3열 주석 → strict / ambiguous 분리, 헤드라인은 **계열 2 + strict 계열 1**. 하드 제외 금지 — 상위에 온 음성은 주석 달아 결과로 보고. §8 질의응답 11번 |
| **채점 주장 과장** (2026-09-17 2차 개정 신규) | 양성은 1상·F2–F3 승인, 음성은 3상 실패 → 증거 수준 비대칭 | “타겟의 진위”가 아니라 “임상 프로그램 관심 배분과의 일치도”로 주장 축소. “파이프라인 자체가 검증됐다”는 문장 사용 금지 |
| **양성 대조군 단위 부족** (2026-09-17 신규) | 독립 프로그램 단위 3개 → 추론 통계 불가 | 기술 통계(단위별 백분위 표)를 주 결과로 사전 선언. 검정은 보조 부기 |
| ChEMBL 데이터 부족 | 활성 < 300 | Mol2Mol 전환 또는 2순위 타겟 |
| 시간 초과 | Day 14에 타겟 미확정 | REINVENT4 포기, “타겟 발굴”로 범위 축소 (여전히 완결된 발표) |

**가장 조심할 것**: 기준을 통과시키려고 숫자를 낮추는 것. 사전 등록한 기준을 발표에서 언급할 예정이므로, 기준을 사후에 바꾸면 그 자체가 가장 큰 감점 요인이 된다. 기준 미달이면 **미달했다고 쓰고 그 이유를 분석하는 쪽**이 점수가 높다.

---

## 10. 참고 문헌 / 링크

**데이터**

- GSE135251 — https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc=GSE135251
- GSE162694 (단계별 구성) — https://www.nature.com/articles/s41598-021-96966-5
- GSE136103 (간경변 scRNA) — https://www.nature.com/articles/s41586-019-1631-3
- GSE202379 (간 snRNA 47명, 간경변 포함) — https://www.nature.com/articles/s41586-024-07465-2

**모델 / 도구**

- Geneformer (Apache-2.0) — https://huggingface.co/ctheodoris/Geneformer
- Geneformer 타겟 발굴 프로토콜 (*Nat Protoc* 2026) — https://www.nature.com/articles/s41596-026-01364-8
- Foundation model in-silico perturbation 한계 벤치마크 — https://arxiv.org/html/2410.13956v2
- REINVENT4 (Apache-2.0) — https://github.com/MolecularAI/REINVENT4
- REINVENT4 논문 (*J Cheminform* 2024) — https://jcheminf.biomedcentral.com/articles/10.1186/s13321-024-00812-5
- Open Targets Platform — https://platform.opentargets.org/
- Open Targets tractability 문서 — https://platform-docs.opentargets.org/target/tractability
- Finan et al. 2017, druggable genome (*Sci Transl Med*) — https://www.ncbi.nlm.nih.gov/pmc/articles/PMC6321762/

**임상 서사**

- MASH 항섬유화 현황 — https://www.frontiersin.org/journals/gastroenterology/articles/10.3389/fgstr.2025.1704078/full
- MASH 간경변 신약개발 리뷰 — https://onlinelibrary.wiley.com/doi/10.1111/liv.70784
- Intrahepatic hypothyroidism / resmetirom 근거 — https://pubmed.ncbi.nlm.nih.gov/38714040/

---

## 11. 사전등록 변경 기록

**모든 변경은 최종 랭킹을 보기 전에 이루어졌다.** 변경 일시·이유·근거를 남기는 것 자체가 사전등록의 핵심이다. 결과를 본 뒤의 변경은 이 절에 넣지 않고 “사후 분석”으로 따로 표기한다. 이 원칙은 §9의 “가장 조심할 것”과 같다.

### 2026-09-15 — 기준군 정의 수정

| 변경 | 이유 |
| --- | --- |
| 주분석 기준군 `F0 + 대조 48명` → **46명** | GEO series matrix 확인 결과 대조군 10명 중 2명(`GSM3998224` F1, `GSM3998341` F2)이 섬유화를 갖고 있었다. 섬유화가 없어야 할 기준군에 F2 환자가 섞이면 앞뒤가 맞지 않는다. DEG를 계산하기 전에 수행 |

### 2026-09-16 — GATE 1 실행 및 결과 기록

| 항목 | 결과 |
| --- | --- |
| 주분석 DEG | 1,737개 (증가 1,315 / 감소 422), 검정 유전자 16,492개 |
| GATE 1 | **통과** — 등급 B 295개(기준 ≥10), 등급 A–C 457개(기준 ≥20) |
| 확인된 문제 1 | **GATE 1이 ECM을 걸러내지 못한다.** 구조 단백질 8개가 등급 B에 잔존 |
| 확인된 문제 2 | **양성 대조군 5개 중 DEG가 0개다**(THRB 포함). 음성에서는 LOXL2·CCR2만 DEG. DEG 목록 위에서는 §2 채점이 성립하지 않는다 |

### 2026-09-17 — §2 채점 설계 개정 (개정 8건 + 유지 결정 1건)

| # | 변경 | 이유 |
| --- | --- | --- |
| 1 | §2 판정 기준을 **p값 → 효과크기**(백분위 + AUC)로 교체 | 사전 검정력 계산 결과, 프로그램 단위로 세면 양성 3 대 음성 4에서 순위가 완벽해도 최소 양측 p = 0.0571. 원래 기준 `p<0.05`는 **통과 불가능한 지뢰**였다 |
| 2 | 음성 대조군 기준을 백분위 ≤ 50 → **≤ 25** | ≤ 50은 무작위 순위로도 0.62–0.69 확률로 충족되는 공짜 기준 |
| 3 | 채점 범위를 **DEG 457개 → 등급 A–C 5,007개** | 양성 대조군이 DEG 목록에 하나도 없음 (2026-09-16 확인) |
| 4 | 대조군을 **약물 프로그램 단위**로 묶고, 단위 점수 = 최상위 구성원 | PPAR 3개는 lanifibranor 하나, CCR2/5는 cenicriviroc 하나, CASP 계열은 emricasan 하나다. 유전자 수로 세면 독립성이 없는 증거를 부풀린다. 같은 논리로 COL4A3/4/5는 α3α4α5 삼합체 1단위 |
| 5 | **치료 방향 규칙**을 전 단계로 확대 (§5 3단계 → §2 채점 전체) | 양성 3단위 중 2단위가 활성화 타겟이고 음성은 전부 억제 타겟이다. 방향 보정 없이는 AUC가 파이프라인 성능이 아니라 방향 차이를 측정한다 |
| 6 | `PNPLA3`·`SERPINH1`을 **out-of-scope 패널**로 분리 | 임상 프로그램이 ASO/siRNA다. 소분자 파이프라인을 siRNA 타겟으로 채점하면 산출물과 채점 기준이 어긋난다. 제외는 결손이 아니라 정답 |
| 7 | `HSD17B13`은 **in-scope 양성 대조군으로 유지** | 소분자 저해제 **INI-822(Inipharm) 1상 착수** 확인 (P1, 2026-09-17). 만약 RNAi 프로그램만 있었다면 6번과 같은 이유로 out-of-scope가 되어 양성이 2단위로 줄었고, 그 경우 최소 p = 0.133으로 검정이 완전히 불가능해졌다 |
| 8 | **음성 대조군 계열 2(구조 단백질 6단위 8유전자)** 신설 | GATE 1이 걸러내지 못한 구조 단백질을 지우는 대신 음성 대조군으로 쓴다. “발현 순위는 올리지만 소분자로는 불가능”이 사전에 확실하므로, GATE 3·4가 강등시키는지가 파이프라인 주장의 직접 시험이 된다. 실패 이유가 다르므로(타겟이 틀림 / 약으로 만들 수 없음) 계열 1과 분리 채점 |
| — | **주분석 대비 `F4 vs F0+대조` 유지** (F3+F4 병합 요구 기각) | 병합하면 DEG 1,737 → 874개, 등급 A–C 후보 457 → 261개로 절반이 된다. “후보 풀을 넉넉히 확보”라는 목적에 역행한다. 풀을 넓히려면 합집합(등급 A–C 525개)이나 연속 점수를 쓰면 되고, **GATE 1이 이미 통과한 뒤 사전등록 주분석을 바꾸는 것은 §9가 경고한 사후 기준 변경에 해당한다** |

**같은 날 함께 기록한 설계 추가**

| 항목 | 내용 |
| --- | --- |
| 후보 공간 | **295개 유지**(축소 안 함) + `sm_evidence` 주석 열(strong 160 / weak 135). 순위 계산에 쓰지 않고 동점 처리와 GATE 5 판단에만 사용 |
| GATE 1 문구 | “ECM을 랭킹 이전에 제거한다”는 주장 **철회**. ECM 방어선은 GATE 3·4가 담당 |
| GATE 3 | **세포 구성 판정** 신설 — 세포형 내부에서 유의하지 않고 비율 변화로 설명되는 유전자는 강등 |
| GATE 5 | 진입 요건에 **High-Quality Ligand 보유** 추가(`strong`으로는 불충분 — 39개가 포켓 근거만 보유) |
| 추가 대비 | **F4 vs F3**(14 vs 54), **단계 추세**(NAFLD 206명, 스플라인 LRT로 말기 급증 패턴) |
| wet lab | **수행하지 않음**을 명시. 도킹을 선택 → **필수**로 승격 |

### 2026-09-17 (2차) — 음성 대조군 라벨 정제 (개정 6건)

**변경 시점**: GATE 4는 Day 9–12이고 **순위가 아직 존재하지 않는다.** 따라서 이 개정은 §11 원칙(“모든 변경은 최종 랭킹을 보기 전에”)을 충족한다. 순위를 본 뒤 음성 기준을 느슨하게 하면 §9가 경고한 사후 완화가 되므로, **기준을 완화하는 방향의 변경은 오늘 이후로는 하지 않는다.**

**변경 계기**: “임상 실패한 약물의 타겟을 음성 대조군으로 쓰는 것이 타당한가 — 실패는 화합물 독성이나 임상 설계 탓일 수도 있고, 타겟은 옳았을 수도 있다”는 지적. 검토 결과 **지적이 타당하고, 계획서에 실제 비일관성이 있었다.**

| # | 변경 | 이유 |
| --- | --- | --- |
| 1 | 음성 단위마다 **주석 3열 추가**(실패 약물 양식 / 실패 사유 / 표적 결합 입증 여부) + 단위당 근거 문헌 1줄 | 임상 실패는 프로그램 실패다. 타겟에 대한 정보를 주는 것은 “결합 입증 + 유효성 없음” 한 경우뿐이고, 독성·PK·환자 선택·상업적 중단은 타겟에 대해 말하지 않는다. 개정 전 계열 1 표에는 “simtuzumab 실패”처럼 **사유가 적혀 있지 않았다** |
| 2 | 계열 1을 **strict / ambiguous로 분리**. strict 요건 = 경구 소분자 + 유효성 실패 + 결합 입증 | 라벨 오염을 없애는 대신 단위를 지우지 않는 방식. 미확인은 ambiguous로 내리고 표에 공개 |
| 3 | **`LOXL2`를 ambiguous로 강등** | ① simtuzumab은 **인간화 IgG4 항체**다. §11 개정 6번은 `PNPLA3`·`SERPINH1`을 ASO/siRNA라는 이유로 out-of-scope로 뺐는데, **같은 규칙을 음성에 적용하지 않은 것은 양식 비대칭**이다. ② epitope가 SRCR4 도메인이고 촉매 부위가 아니며, 항-LOXL2 항체가 촉매 활성·콜라겐 가교를 저해하지 못한다는 보고가 있어 결합 자체가 의심된다. ③ 경구 범-LOX 소분자 PXS-5505(골수섬유증 1/2a상), LOXL2 선택적 PAT-1251은 **아직 실패하지 않았다** |
| 4 | **헤드라인 음성 집합 = 계열 2(6단위) + strict 계열 1(3단위)**, 민감도 2종(LOXL2 포함 / 계열 1만) 병기 | 계열 2는 “타겟이 틀렸다”는 주장이 아니라 “소분자 산출물 파이프라인의 오답”이라는 **정의**이므로 라벨 오염 비판에 면역이고, 양식도 일치하며 단위 수도 많다. 대가는 27쌍(0.037 간격)으로 30쌍(0.033)과 실질 차이 없음. 단 **strict 계열 1 단독 판정은 불가능**하다(3 대 3, 최소 양측 p = 0.10) |
| 5 | **하드 제외 금지 + 제외 규칙 명문화** — 대조군은 채점용이며 신규 제안에서 빠지지만 그 근거는 **순환논리 회피와 신규성**이고 타겟 유효성 판단이 아니다. 음성이 상위에 오면 강등하지 않고 주석 달아 결과로 보고 | 개정 전 이 규칙은 “그 다음 순위의 **미지** 타겟이 우리의 제안이다” 한 구절에만 암시돼 있어, “실패한 타겟이라서 버렸다”로 오독될 수 있었다. 또 음성이 상위에 오는 것은 “임상 합의가 맞다”는 가정 하에서만 오류이며, **합의를 완벽히 재현하는 파이프라인은 아무것도 발견하지 못한다** |
| 6 | **채점 주장 축소** — “파이프라인 자체가 검증된 것” 문장 **사용 금지**, “순위가 임상 성적표와 무작위 이상으로 일치한다”로 교체 | 양성(`HSD17B13` 1상 = 유효성 미입증, `THRB` = F2–F3 승인이고 F4는 라벨 제외)과 음성(2b/3상 실패)의 증거 수준이 비대칭이다. 이 채점이 측정하는 것은 타겟의 진위가 아니라 임상 프로그램 관심 배분과의 일치도다 |

**함께 갱신한 곳**: §2 계열 1 표·판정 기준 4–5번·사전 검정력 박스·대조군 역할 규칙 신설 / §4 GATE 4 통과 기준 / §6 Day 1 P1 업무 / §8 질의응답 **11번 신설**(기존 11번 → 12번) / §9 위험 2행 추가.

### 2026-09-17 (3차) — 주분석 대비 전환 `F4 vs F0+대조` → `F4 vs F0` + GSE162694 데이터 정의 정정

**변경 시점과 투명성**: 이 변경은 **구 정의로 GATE 2 지표를 계산해 본 뒤**에 이루어졌다(구 정의 결과: 상위 500 방향 일치 89.6%, 이항 p = 6.1e-80, Spearman ρ = 0.616 → 통과). 따라서 “결과를 보기 전의 변경”이라고 쓰지 않는다. 다만 ① 구 정의로도 GATE 2는 **통과**했으므로 게이트를 통과시키려는 변경이 아니고, ② GATE 3·4를 실행하지 않아 **최종 순위는 아직 존재하지 않는다**. 구 정의 결과는 지우지 않고 민감도 3으로 함께 공개한다.

**변경 계기**: GSE162694 독립 DEG가 GSE135251보다 비정상적으로 많았다(DEG 11,475 vs 1,737). 원인을 검토하던 중 **기준군 조성이 두 코호트에서 다르다**는 지적이 나왔다. 두 코호트 모두 기준군이 “F0 + 정상”이라는 같은 이름이었지만, 기준군 안의 정상 간 비율은 GSE135251 8/46 = **17%**, GSE162694 31/66 = **47%**로 약 3배 차이였다. 정상 간 비율이 높으면 기준군의 기저 발현이 대사질환 상태에서 건강 상태 쪽으로 이동하고, F4와 비교할 때 섬유화 유전자뿐 아니라 정상 간의 대사·항상성 유전자까지 DEG로 잡힌다. 이름만 같고 실제로는 다른 집단을 비교한 것이다.

| # | 변경 | 이유 |
| --- | --- | --- |
| 1 | **주분석 대비 `F4 vs F0+대조` → `F4 vs F0`** (GSE135251 14 vs 38, GSE162694 12 vs 35) | 두 코호트의 기준군을 “MASLD는 있으나 섬유화 0”이라는 같은 집단으로 맞춘다. 섬유화 진행만 비교하므로 “섬유화 타겟 발굴”이라는 목적에도 더 맞다. §4 GATE 1 통과 조건 문구(“F4 vs F0 DEG”)와도 일치하게 된다 |
| 2 | 구 주분석을 **민감도 3으로 강등**(삭제하지 않음) | 전환 전 결과(GATE 1 통과, GATE 2 구 정의 지표)를 그대로 공개해야 “게이트 통과 후 정의를 바꿨다”는 비판에 답할 수 있다 |
| 3 | **GATE 1·2를 신 주분석으로 재실행**. 판정 기준(B ≥ 10, A–C ≥ 20 / 방향 일치 ≥ 70%, ρ ≥ 0.3)은 **바꾸지 않는다** | 대비가 바뀌면 DEG 목록이 바뀌므로 게이트 판정도 새로 해야 한다. 기준 완화 금지 원칙(2차 개정)은 그대로 유지 |
| 4 | §3 GSE162694 구성 정정: `F0 66 / 정상 31` → **F4 12 / F3 8 / F2 27 / F1 30 / F0 35 / 정상 31 = 143** | GEO series matrix `fibrosis stage` 필드 집계 결과. 개정 전 표는 F1·F2를 빠뜨리고 F0 수를 틀리게 적었다. 재현 기준군 `F0 + 정상 n=97`도 실제로는 66명이다 |
| 5 | §3·§6 GSE162694 설계 `~batch + group` 필수 → **`~group`** | GEO series matrix 확인 결과 플랫폼은 HiSeq 3000(GPL21290) **하나뿐**이고(개정 전 “HiSeq 3000·4000 2개 플랫폼”은 오기), 배치를 나타내는 필드가 **없다**. 논문(Pantano et al. 2021, *Sci Rep*) 보충자료에 배치 정보가 있는지는 확인하지 못했다 — 확인되면 그때 `~batch + group`으로 되돌리고 이 절에 기록한다 |

**1차 개정 “주분석 유지” 결정과의 관계**: 같은 날 1차 개정의 유지 결정(“GATE 1이 이미 통과한 뒤 사전등록 주분석을 바꾸는 것은 사후 기준 변경”)과 충돌한다. 그때 기각한 것은 **후보 풀을 늘리려는** 대비 변경(F3+F4 병합)이었고, 이번은 두 코호트 사이의 **교란(기준군 조성 불일치)을 바로잡는** 변경이다. 이 구분이 설득력이 있는지는 심사자가 판단하도록 두 기록을 모두 남긴다.

**실행 현황 (2026-09-17)**: GSE135251 DESeq2 재실행 완료(§3 표). GSE162694 신 주분석 DESeq2, GATE 1 재실행, GATE 2 재실행은 **미실행**.

**함께 갱신한 곳**: §0 주분석·GATE 1 결과 행 / §3 데이터 표·대비 정의·실행 결과 / §4 GATE 1 판정 결과 주의 문구·GATE 2 비교 대비 / §2 채점 범위 이유 문구 / §6 Day 4 / §8 질의응답 1·2번.

### P1(간 전공) 검토 대기

- **(신규, 최우선) 음성 strict 3단위의 표적 결합 입증 여부** — selonsertib(ASK1), cenicriviroc(CCR2/5), emricasan(CASP)에 각각 PD 바이오마커로 결합을 보인 근거가 있는가. 없으면 해당 단위도 ambiguous로 내려간다. 미확인으로 남을 경우 헤드라인 음성이 계열 2의 6단위만 남을 수 있다
- **(신규) 세 단위의 실패 사유가 유효성인지 재확인** — 독성·등록 중단·상업적 판단이 섞여 있으면 strict 요건 ②가 깨진다
- CASP 계열 9개를 emricasan 1단위로 묶는 것이 타당한가 (§2 계열 1)
- PPAR 계열을 “실패”로 볼지 “부분 성공”으로 볼지 — 애매하면 대조군에서 빼고 중립으로 (§2 원문 주의사항)
- 구조 단백질 8개가 “소분자 프로그램이 존재하지 않는다”는 전제가 모두 맞는가 (특히 `POSTN`, `FLNC`)

### 미결 항목 (아직 사전등록하지 않음 — 승인 후 이 절에 편입)

선행연구 4편의 방법론 검토에서 나온 제안들. 채택 여부를 정하지 않았으므로 **아직 계획이 아니다.**

1. **선행연구 후보 재평가 패널** — 선행연구 4편이 지목한 25개 유전자를 우리 결과에서 조회한 결과(2026-09-17 예비 계산): 논문 1의 “표적 가능” 8개 중 **5개가 소분자 등급 없음**(CCL19·CXCL6·CXCL9·LUM·JCHAIN — DGIdb는 항체·간접 상호작용까지 포함), 논문 2의 보존 허브 6개 중 **DEG 0개**, 논문 3의 중심 유전자 `EGR1`은 **DEG 아님**(log2FC −0.09, p=0.42), 논문 4의 `NQO1`은 등급 A DEG로 재현. 채택하면 wet lab 없이 파이프라인 신뢰도를 보이는 자료가 된다
2. **표준 방법 대비 우위 입증** — STRING PPI 연결중심성 순위를 §2 대조군으로 동일하게 채점해 비교 (논문 2·3·4가 모두 이 방법). Geneformer 학습 중 CPU로 실행 가능
3. **GO/KEGG/GSEA 농축 분석** — 순위에는 쓰지 않고 생물학 서사용. `geo` 환경에 clusterProfiler 미설치
4. **즉시초기유전자 QC** — `FOS`·`JUN`·`EGR1`·`HSPA1A` 계열을 표시. 조직 채취 후 허혈 시간 편차를 반영할 수 있음(우리 데이터에서 `FOS` log2FC −3.42, p=0.017로 효과크기 대비 p값이 약함)
5. **채택하지 않기로 검토된 것** — ComBat 병합(논문 1·2), cytoHubba 허브를 후보 선정에 사용(논문 2·3·4), CIBERSORT LM22(간세포·HSC·담관세포 없음), 발굴 데이터 내 진단 AUC(순환 논리), GEPIA/TIMER 생존 분석(간암 질문), miRNet, Phyre2, 외부 데이터셋 대조군 차용(논문 3 — 배치와 그룹이 완전히 겹침), WGCNA(단계 추세와 역할 중복), HBV 코호트 추가(MASH F4 특이성 희석)