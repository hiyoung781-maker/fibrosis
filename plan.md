# 간섬유화 표적의 약물성 재평가 → REINVENT4 Lead 도출

## 선행 표적 목록 차용 + 독립 검증 + 약물성 게이트 · 2인 · 14일

> **버전** v2.0 (2026-09-18 개정) — v1.0의 자체 DEG 파이프라인을 **선행 논문 표적 목록 차용 + 독립 코호트 검증**으로 교체
> **전신 프로젝트** `/home/seyoung/academic_symposium/fibrosis/` (F4 특이 표적 발굴) — **폐기, 회생하지 않음**
> 폐기 사유: 자체 분석에서 F4 고유 전사 프로그램 부재를 확인 (F4 vs F3 padj<0.05 = **52개**, `late_stage_surge` = **60 / 15,395**, `amplification off` = **0 / 15,410**)
> **재사용 자산**: 임상 성적표 채점 설계, Open Targets tractability 파이프라인, REINVENT4/Geneformer 설치본, GSE162694·GSE202379 로컬 데이터, MSigDB Hallmark 로컬 사본

---

## 0. 확정 사항

| 항목 | 결정 |
| --- | --- |
| **적응증** | **만성 간질환의 간섬유화 — 병인 무관** |
| **핵심 질문** | 이미 제안된 간섬유화 표적들은 **약이 될 수 있는가** |
| **표적 후보 풀** | **선행 논문 2편에서 차용** (§4). 자체 전장 DEG 수행하지 않음 |
| **차용의 정당화** | ① 두 논문이 쓰지 않은 **독립 코호트(GSE162694)** 에서 검증 ② 두 논문이 하지 않은 **연령·성별 보정** 적용 ③ 두 논문이 하지 않은 **약물성 평가** 수행 |
| **주 산출물** | **표적 약물성 스코어카드(§5.4)** + **소분자 선도 물질(§9)** |
| 공변량 | **연령 + 성별 보정**. BMI·당뇨는 매개변수이므로 보정하지 않음 |
| 병기 처리 | **이분 분할 금지.** 병기를 순서형 수치(0–4)로 두고 단조 추세 검정 |
| AI 모델 | **REINVENT4**(주) + **Geneformer**(세포형 귀속 보조). 재학습 없음 |
| Geneformer 역할 | **표적 선정에 쓰지 않음.** 세포형 귀속 + 문헌 일치 확인 패널 1개 (근거 §12 R1) |
| 킬 게이트 | Day 1 / Day 3 / Day 6 / Day 9 |
| wet lab | 수행하지 않음 |

> **가정**: 잔여 기간 **14일**. 다르면 §11만 조정한다.

---

## 1. 이 연구를 한 문장으로

> **"간섬유화 표적 유전자 목록은 이미 여러 편 나와 있다. 그런데 서로 일치하지 않고, 어느 논문도 그 유전자가 약이 될 수 있는지는 묻지 않았다. 본 연구는 2026년 발표된 두 편의 표적 목록을 독립 코호트에서 연령·성별 보정 하에 검증하고, 약물성 기준으로 재평가한 뒤, 살아남은 표적에 대해 생성형 AI로 소분자 선도 물질을 설계한다."**

---

## 2. 문제의식 — 두 겹

### 2.1 첫째 겹: 승인약은 섬유화를 못 고친다

| 시험 | 약 | 표적 축 | 섬유화 1단계 이상 개선 | 위약 |
| --- | --- | --- | --- | --- |
| MAESTRO-NASH | resmetirom 100 mg | THR-β (**대사**) | **25.9%** | 14.2% |
| MAESTRO-NASH | resmetirom 80 mg | THR-β (**대사**) | 24.2% | 14.2% |
| ESSENCE | semaglutide | GLP-1R (**대사**) | 위약 대비 약 **14%p** | — |

**환자 4명 중 3명은 섬유화가 개선되지 않는다.**

**전사체 수준 방증** (전 프로젝트에서 확인): 양성 대조군 5개(`THRB` `PPARA` `PPARD` `PPARG` `HSD17B13`) 중 주분석 DEG는 **0개**였다. **승인약 표적은 섬유화 병기와 함께 발현이 변하지 않는다.** 즉 이 약들은 섬유화 축 위에 있지 않다. 그래서 25%다.

### 2.2 둘째 겹: 표적 목록은 이미 많은데, 서로 안 맞고 약물성을 아무도 안 봤다

2026년 발표된 두 편:

```
Li  et al.  (Sci Rep)        : AQP1  CCL19  CXCL6  CXCL9  CXCL10  EPCAM  IGJ  LUM
Yang et al. (Can J Gastro)   : MAOA  LOC102724200  SLC16A3  GPM6B  CST7  MT3  ZNF142
──────────────────────────────────────────────────────────────────────────────────
교집합                        : ∅
```

두 논문은 **GSE84044와 GSE14323을 공유**한다. 즉 데이터의 절반이 같다. 그런데 헤드라인 유전자가 **하나도 겹치지 않는다.** 결과를 지배한 것은 데이터가 아니라 역치·WGCNA 모듈·hub 선정 알고리즘이다.

그리고 **두 논문 모두 약물성(druggability)을 평가하지 않았다.**

> **이 두 겹이 연구의 존재 이유다. 포스터 introduction은 이 두 문단으로 끝난다.**

---

## 3. 차용하는 것과 우리가 하는 것

| 단계 | 출처 |
| --- | --- |
| 표적 후보 풀 | **차용** — 선행 논문 2편 |
| 독립 코호트 검증 (미사용 코호트 + 공변량 보정) | **우리** |
| 약물성 게이트 (Open Targets tractability) | **우리** — 두 논문 모두 미수행 |
| 임상 성적표 채점 (실패 표적·구조 단백질 대조군) | **우리** |
| 세포형 귀속 (어느 세포를 겨냥해야 하는가) | **우리** |
| 소분자 설계 + 도킹 검증 + 물성·신규성 분석 | **우리** |

**차용은 한 단계뿐이고 나머지 다섯 단계가 우리 것이다.** 포스터 방법 섹션 첫 줄에 이 표를 그대로 넣어 "무엇을 가져왔는지"를 먼저 공개한다. 숨기면 질의응답에서 무너지고, 먼저 밝히면 설계의 일부가 된다.

---

## 4. 입력 자산

### 4.1 차용 표적 풀

| 출처 | 데이터셋 | 대비 | 공변량 | 산출 |
| --- | --- | --- | --- | --- |
| **Li et al.**, *Sci Rep* 2026, `10.1038/s41598-026-45080-5` | GSE14323 + GSE84044 (limma+sva 병합) | 섬유화 vs 비섬유화 (이분) | **명시 없음** | **8개**. DEG ∩ WGCNA 모듈 ∩ druggable gene. 최종 `AQP1` (AUC>0.90), **wet lab 검증**: IHC n=10 vs 10, LX-2 HSC에서 knockdown → TGF-β 유도 활성화 억제 |
| **Yang et al.**, *Can J Gastroenterol Hepatol* 2026, `10.1155/cjgh/9254321` | GSE135251 + GSE84044 + GSE197112 + GSE14323 | 섬유화/간경변 vs 대조 (이분) | **체계적 미사용** (GSE84044만 연령 층화) | **7 hub** + **병인 공통 상향 26개**. 코호트별 상향 301–512개 |

**풀 = Li 8개 ∪ Yang 26개 (7 hub 포함) — 최대 34개 (중복 시 감소)**

> **Day 1 확인 필수 (UNVERIFIED)**: Yang et al.의 **26개 전체 목록**과 코호트별 301–512개 표가 supplementary로 공개되는지. 확보 실패 시 §10 G1의 대체 규칙 적용.

### 4.2 검증 코호트 — **GSE162694 (양 논문 모두 미사용)**

```
Li  et al. : GSE14323, GSE84044
Yang et al.: GSE135251, GSE84044, GSE197112, GSE14323
GSE162694  : 어느 쪽도 사용하지 않음  ← 진정한 독립 검증
```

로컬 보유. **연령·성별 메타데이터 보유** (두 논문 모두 보정하지 않은 변수).

**최종 분석 표본 = F0 35 + F1 30 + F2 27 + F3 8 + F4 4(생검) = 104명**

제외 근거 (모두 실제 파일에서 직접 계산):

**① `normal histology` 31명 제외** — 성비 **3M/28F(여성 90%)** 로 F0(10M/25F)와 크게 다르고, 섬유화 병기가 아닌 별개 모집단.

**② 이식 적출 의심 F4 8명 제외** — 라벨이 아니라 측정으로 근거를 제시한다:

| 그룹 | n | 라이브러리 크기 중앙값(M) | 범위 | 검출 유전자 중앙값 |
| --- | --- | --- | --- | --- |
| **F4 NAS 결측(적출 의심)** | 8 | **59.09** | **43.5 – 65.1** | **30,333** |
| F4 생검 | 4 | 38.16 | 34.9 – 42.6 | 30,624 |
| F0 (NAS 보유) | 26 | 31.47 | 20.9 – 42.4 | 31,168 |
| 정상 조직 | 31 | 29.76 | 22.0 – 42.0 | 31,120 |
| F1–F3 | 65 | 30.75 | 21.2 – 56.1 | 31,240 |

- 적출 의심 8명의 깊이(43.5–65.1M)가 F0 26명(20.9–42.4M)과 **단 하나도 겹치지 않는다** → 기술 변수와 완전 교락
- **2배 깊게 읽었는데 검출 유전자는 오히려 적다**(30,333 < 31,168) → 전사체 복잡도 저하 = 조직 손상의 정량 지표
- **포스터 QC 패널로 그대로 사용**

**③ 연령 교락이 심각하므로 반드시 보정** (직접 계산)

| 병기 | n | 연령 중앙값 | 범위 | 남/여 |
| --- | --- | --- | --- | --- |
| F0 | 35 | 48 | 20–72 | 10 / 25 |
| F1 | 30 | 45 | 21–68 | 10 / 20 |
| F2 | 27 | 45 | 18–64 | 8 / 19 |
| F3 | 8 | 51 | 32–62 | 4 / 4 |
| F4 | 12 | **62** | **48–69** | 5 / 7 |

F4는 전원 48세 이상, 중앙값이 F0보다 14세 높다.

### 4.3 세포형 참조 — GSE202379

MASLD snRNA-seq, 59샘플, SAF 병기 + 연령 + 성별 보유. 로컬. 간세포 / 담관세포 / HSC·근섬유아세포 / 내피 / 면역 마커 도출에 사용.

### 4.4 사용하지 않는 것

- **GSE135251** — 연령·성별 메타데이터가 **둘 다 없음**(characteristics 5줄 전부 `nas score / fibrosis stage / group in paper / disease / Stage`). 공변량 보정이 요건인 이상 검증 코호트로 부적합. 또한 Yang et al.이 이미 사용 → 독립성도 없음.

---

## 5. 표적 재평가 — 이 계획의 핵심

### 5.1 독립 코호트 검증 (Day 2)

차용 풀(≤34개)**만** GSE162694에서 검정한다. 전장 DEG를 돌리지 않으므로 계산은 수 분이고 다중검정 부담도 작다.

```r
# 병기를 순서형 수치로. 이분 분할하지 않는다.
dds <- DESeqDataSetFromMatrix(counts, cd, ~ age_z + sex + stage_num)   # n = 104
dds <- dds[rowSums(counts(dds) >= 10) >= 10, ]      # 고정 임계값 k=10
dds <- DESeq(dds, test = "LRT", reduced = ~ age_z + sex)
res <- results(dds)[pool_ensembl, ]                  # 차용 풀만 추출
```

**민감도 분석 (필수)**: `~ stage_num`(무보정) 모형을 함께 돌려 **연령·성별 보정 전후로 판정이 바뀌는 유전자**를 명시한다.
→ **"선행 논문이 보정하지 않았기 때문에 놓쳤거나 과대평가한 유전자"** 목록이 되며, 이것이 검증 파트의 헤드라인 결과다.

> **분할하지 않는 이유**: F3는 8명, F4 생검은 4명이다. 이분 대비면 검정력이 무너지지만, 추세 모형은 **104명 전체에서 정보를 빌린다.** 동시에 "왜 이 경계인가"라는 질문이 소멸한다.

> **필터 임계값을 k=10으로 고정**한다. 전 프로젝트는 `min(n_case, n_ref)`에 묶어 **같은 데이터에서 k=4일 때 29,991개, k=26일 때 21,858개**가 검정됐다(직접 계산). 추세 모형에는 case/ref가 없으므로 고정값을 쓴다.

### 5.2 세포형 귀속 (Day 3)

GSE202379 snRNA에서 각 후보의 발현 세포형을 확인한다. **약이 어느 세포에 도달해야 하는지를 정하는 단계이고, 곧바로 §9의 의약화학 요건이 된다.**

| 귀속 세포형 | 해석 |
| --- | --- |
| **HSC / 근섬유아세포** | **항섬유화 표적** — 우리가 찾는 것 |
| 간세포 | 대사 축일 가능성 → 병인 무관 주장 재검토 필요 |
| **담관세포** | **ductular reaction 표지자** — 전 프로젝트에서 음성 대조군으로 분류한 `KRT7`과 동일 계열. `EPCAM`이 여기 해당할 것으로 예상 |
| 면역세포 | 분비 리간드(케모카인)일 가능성 → 수용체를 겨냥해야 함 |

### 5.3 약물성 게이트 (Day 4)

Open Targets Platform GraphQL. **버킷 정의는 ChEMBL 팀이 유지하는 공개 파이프라인(`chembl/tractability_pipeline_v2`)의 산출값이며, 우리가 매긴 점수가 아니다.**

| Bucket | 정의 |
| --- | --- |
| SM_B1 | Approved Drug |
| SM_B2 | Advanced Clinical |
| SM_B3 | Phase 1 Clinical |
| **SM_B4** | **Structure with Ligand** ← 설계 착수 하한 |
| SM_B5 | High-Quality Ligand |
| SM_B6 | High-Quality Pocket |
| SM_B7 | Med-Quality Pocket |
| SM_B8 | Druggable Family |

**SM_B4 하한의 근거는 임의값이 아니다**: B4 이상이면 구조와 리간드가 동시에 확보되므로 **도킹 기반·리간드 기반 scoring이 둘 다 가능**하다. 즉 이 게이트를 통과한 표적은 REINVENT4가 반드시 작동한다. **컷오프가 하류 실현가능성에서 역산된 값이다.**

`datatypeScores`를 분리해 **`genetic_association`을 따로 본다.** `literature` 점수가 높은 것은 논문이 많다는 뜻이지 표적 근거가 아니다.

```graphql
query {
  disease(efoId: "<간섬유화/간경변 EFO id — Day 1 확정>") {
    associatedTargets(page:{index:0,size:500}) {
      rows { score
             datatypeScores { id score }
             target { id approvedSymbol
                      tractability { id modality value } } }
    }
  }
}
```

### 5.4 산출물 — 표적 약물성 스코어카드

**풀이 34개 이하이므로 "퍼널"이 아니라 "완전한 표"를 만든다.** 모든 행의 모든 칸을 채운다. 후보가 줄어드는 그림보다 이쪽이 정직하고 포스터에서 더 잘 읽힌다.

| 유전자 | 출처 | 분자 종류 | GSE162694 추세 (보정) | 보정 전후 변화 | 세포형 | OT genetic | OT literature | **SM bucket** | 구조/리간드 | **판정** |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `AQP1` | Li | 수분 채널 | | | | | | | | |
| `MAOA` | Yang | 효소 (FAD) | | | | | | | | |
| `SLC16A3` | Yang | 수송체 | | | | | | | | |
| `LUM` | Li | ECM 프로테오글리칸 | | | | | | | | |
| `EPCAM` | Li | 상피 표면 항원 | | | | | | | | |
| `CXCL9/10`, `CXCL6`, `CCL19` | Li | **분비 리간드** | | | | | | | | |
| `MT3`, `GPM6B`, `ZNF142`, `CST7`, `IGJ` | Yang/Li | | | | | | | | | |
| `LOC102724200` | Yang | **미특성 lncRNA** | | | | | | | **out-of-scope** | |
| (Yang 26개 나머지) | Yang | | | | | | | | | |

**사전 예상 (INFERENCE — Day 4에 Open Targets로 확정, 결과가 다르면 그대로 보고)**

- **`MAOA`** — 승인 약물 다수(moclobemide, phenelzine 등), 결정 구조 확보, MAO-A/MAO-B 선택성이 고전적 의약화학 과제 → **SM_B1 예상, 최우선 후보**
- **`SLC16A3`(MCT4)** — MCT 저해제 화학이 종양학에 존재 → 중간~높음
- `AQP1` — aquaporin은 승인 약물이 없고 공극이 좁아 선택적 저해제 확보가 어려움 → **낮음**
- 케모카인 4종(`CXCL6/9/10`, `CCL19`) — **분비 리간드는 소분자 표적이 아니다.** 약이 되려면 수용체(CXCR3, CXCR1/2, CCR7)를 겨냥해야 함
- `LUM` — 구조 단백질. 전 프로젝트 음성 대조군 계열 2(`COL1A1` `POSTN` 등)와 동일 성격
- `EPCAM` — 담관세포 표지자. §5.2에서 담관세포 귀속이 확인되면 ductular reaction 반영
- `IGJ` `MT3` `GPM6B` `ZNF142` `CST7` — 낮음
- `LOC102724200` — 미특성 lncRNA. **소분자 파이프라인에서 out-of-scope로 분류.** 제외는 결손이 아니라 **정답 출력**이다 (전 프로젝트 `PNPLA3`(ASO)·`SERPINH1`(siRNA) 처리와 동일 규칙)

### 5.5 AQP1 — 버리지 말고 이렇게 쓴다

Li et al.의 헤드라인 `AQP1`은 **wet lab 검증까지 있어 표적 유효성 근거가 이 풀에서 가장 강하다.** 그런데 약물성은 가장 낮을 것으로 예상된다.

> **`AQP1`은 "표적 유효성이 가장 강한데 약물성이 가장 낮은" 사례로 포스터에 넣는다.**
> 표적 유효성(target validation)과 약물성(druggability)이 **별개의 축**이라는 것을 보여주는 이 풀 최고의 예시다. 버리는 것보다 이렇게 쓰는 편이 훨씬 강하다.

이것이 포스터 고찰의 첫 항목이 된다: *"좋은 표적이 곧 좋은 약은 아니다."*

---

## 6. 임상 성적표 채점 — 우리 필터를 우리가 채점한다

전 프로젝트 설계 계승: **양식 일치(modality matching), strict/ambiguous 분리, 약물 프로그램 단위 묶음, 방향 규칙.**

### 6.1 대조군

| 계층 | 정의 | 단위 |
| --- | --- | --- |
| **P1** | 간섬유화 적응증 **승인약** 표적 | **없음 ← 이것이 §2.1의 미충족 수요 그 자체** |
| **P2** | 간섬유화 임상에서 섬유화 평가변수 충족, 또는 타 장기 섬유화 임상 진행 중인 소분자 표적 | `NR1H4`(OCA, REGENERATE) `ITGAV`/`ITGB6`(bexotegrast) `NOX1`/`NOX4`(setanaxib) |
| **P3** | 전임상 HSC 활성화 구동 검증 | `TGFB1` `TGFBR1` `PDGFRB` `YAP1` `BRD4` |
| **N-임상실패** | 경구 소분자 / 유효성 실패 / 표적 결합 입증 3조건 충족 = strict | `MAP3K5`(selonsertib) `CCR2` `CCR5`(cenicriviroc) `CASP1/2/3/4/6/7/8/9/10`(emricasan) |
| **N-ambiguous** | 양식 불일치 또는 결합 미입증 | `LOXL2`(simtuzumab = **항체**) |
| **N-구조단백질** | 발현 순위가 무조건 올리는 유전자군 | `COL1A1` `COL4A3/4/5` `KRT7` `TPM1` `POSTN` `FLNC` |
| **out-of-scope** | 소분자 불가 | `PNPLA3`(ASO) `SERPINH1`(siRNA) `LOC102724200`(lncRNA) |

**P1이 비어 있다는 사실을 그대로 쓴다.** "간섬유화에는 승인된 항섬유화제가 없다"가 연구의 존재 이유다.

### 6.2 채점 방식 — 기대를 정직하게 낮춘다

**임상 실패 표적(`LOXL2` 등)과 구조 단백질(`COL1A1` `LUM` 등)은 섬유화에서 실제로 강하게 증가한다.** 따라서 *"우리 발현 순위가 이들을 아래로 내린다"* 를 기대하면 안 된다.

대신 **대조군을 차용 풀과 동일한 스코어카드에 통과시켜, 어느 열이 실제로 이들을 걸러내는지 보인다.**

```
                 GSE162694 추세  세포형      SM bucket   판정
LOXL2              상위?          HSC?          B         ?
COL1A1             상위?          HSC           ?         구조단백질
LUM (차용 풀)       상위?          ?             ?         ?
MAP3K5             ?              ?             A         임상실패
NR1H4              ?              간세포        ?         P2
```

**이 표가 결과다.** "발현만으로 표적을 고르면 안 되는 이유"를 우리 데이터로 보여준다. 어느 쪽 결과든 발표 가능하다.

**사전 등록 (Day 0)**: 대조군 세트·단위 묶음·방향 규칙·판정 기준을 파일로 커밋하고 **결과를 본 뒤 수정하지 않는다.** 포스터에 "pre-registered" 명시.

---

## 7. 경로 데이터베이스의 역할

풀이 34개 이하이므로 **과대표현 분석(ORA)은 검정력이 없다.** 역할을 다음 두 가지로 한정한다.

| 임무 | DB | 산출물 |
| --- | --- | --- |
| **① 후보별 경로 소속 주석** | KEGG (`hsa04350` TGF-β, `hsa04933` AGE-RAGE 등), Reactome (ECM organization, Collagen formation), MSigDB Hallmark (**로컬 보유**) | 스코어카드에 "경로 소속" 열 추가. 후보가 섬유화 핵심 경로에 있는지, 주변부인지 |
| **② 핵심 vs 곁가지 판별** | **Pathway Commons** | 후보에서 섬유화 effector(`TGFB1` `COL1A1` `ACTA2`)까지의 **네트워크 최단 거리**. 풀이 작을수록 이 지표가 잘 작동한다 |

**명시**: 경로 DB는 표적을 선정하지 않는다. 선정은 §5.1 검증 × §5.3 약물성이 한다. 경로 DB는 **해석의 언어**와 **핵심/주변 판별**을 제공한다.

---

## 8. Geneformer의 역할 — 한정

**표적 선정에 쓰지 않는다** (근거 §12 R1). 두 가지 용도로만 쓴다.

1. **세포형 귀속 보조** (§5.2) — Geneformer 세포 임베딩으로 GSE202379 세포형 분류를 보조. scISP 벤치마크에서 Geneformer가 실제로 통과한 과제는 cell state separation(96/224)이지 perturbation 추론(14.73%)이 아니다.
2. **문헌 일치 확인 패널 1개** — 최종 후보에 대한 in silico perturbation 결과가 문헌 방향과 일치하는지. **캡션에 한계를 한 줄 명시**하고, 표적 선정 근거로 쓰지 않는다.

**예상 질의 대비 답변**: *"zero-shot in silico perturbation의 표적 예측 정확도는 최근 벤치마크에서 15% 수준으로 보고되어, 저희는 이를 표적 선정 근거로 쓰지 않고 문헌 일치 확인용으로만 사용했습니다."* — 약점을 미리 알고 통제했다는 것이 가점 요인이다.

> **시간이 부족하면 Geneformer를 빼고 REINVENT4를 제대로 한다.** 두 모델을 어설프게 돌리는 것보다 한 모델을 끝까지 미는 쪽이 심사기준(연구 내용의 타당성)에서 유리하다.

---

## 9. REINVENT4 — 프로젝트의 무게중심

### 9.1 표적 확정

§5.4 스코어카드에서 **SM_B1–B4이면서 §5.1 검증을 통과한 후보** 중 세포형 귀속이 항섬유화에 부합하는 것. 복수면 리간드/구조 확보 상태가 좋은 쪽.

### 9.2 설계

1. **PDB 구조 + 공개 리간드 확보** (SM_B4가 보장)
2. **도킹 프로토콜 검증 — 필수 선행**
   - 활성 화합물 vs 비활성/약활성 화합물을 동일 포켓에 도킹해 **점수가 분리되는지** 확인
   - 분리됨 → 도킹 점수를 scoring component로 사용
   - 분리 안 됨 → **도킹 제외**, 리간드 유사도 + 물성만 사용. **그리고 그 사실을 결과로 보고**
   - *이것은 ML 검증이 아니라 의약화학의 표준 대조 실험이다. "당신들 점수가 무엇을 의미하는지 어떻게 압니까"에 대한 정답이고, 비용은 도킹 2회다.*
3. **REINVENT4 실행**
   - **Libinvent (R-group replacement)** 우선 — 공개 리간드 스캐폴드 변형. de novo보다 안정적
   - scoring: 도킹(검증 통과 시) + QED + **SA score ≤ 4** + MW ≤ 500 + logP 2–5 + TPSA
   - `MAOA`가 표적이 될 경우 **MAO-A / MAO-B 선택성**을 명시적 scoring 목표로 추가 → 고전적 의약화학 과제를 직접 다루게 된다
4. **산출**: 생성 분자 → 필터 → top 30
5. **물성 3자 비교 산점도** (생성 분자 vs 공개 리간드 vs 기존 승인약) ← 포스터 중앙 그림
6. **신규성 정량**: Murcko scaffold 분포 + 기존 계열 대비 최대 Tanimoto 히스토그램

> REINVENT4는 CPU/GPU 각 약 8 GiB면 충분하다. **A100 ×3은 병목이 아니다.** 병목은 표적 확정과 서사이며 일정은 거기에 맞춰 짰다.

---

## 10. 킬 게이트

| 게이트 | 시점 | 통과 조건 | 실패 시 대체 경로 |
| --- | --- | --- | --- |
| **G1 풀 확보** | Day 1 종료 | Yang et al. **26개 전체 목록** 확보 (supplementary 또는 본문) | 헤드라인 **15개**(Li 8 + Yang 7)로 진행하고, 약물성 하한을 **SM_B4 → SM_B6**으로 완화. 완화 규칙은 **Day 0에 미리 기록** |
| **G2 검증** | Day 3 종료 | 차용 풀 중 GSE162694 추세 **padj<0.05가 ≥ 3개** | 명목 p<0.05로 완화하고 **"선행 목록이 독립 코호트에서 재현되지 않는다"를 결과로 보고**. 이 경우에도 §2.2의 문제의식이 오히려 강화되므로 프로젝트는 성립 |
| **G3 약물성** | Day 6 종료 | **SM_B1–B4 후보 ≥ 1개** | SM_B5–B6까지 확장. 그래도 0이면 **"제안된 표적 전체가 소분자로 접근 불가"** 가 결론이 되고, REINVENT4는 §6 P2 대조군(`NOX4` 등) 중 하나를 대상으로 실행해 설계 역량 시연 |
| **G4 도킹** | Day 9 종료 | 활성/비활성 도킹 점수 분리 확인 | 도킹 제외, 스캐폴드 변형 + 물성 scoring만으로 진행 (§9.2-2) |

**모든 완화 규칙은 결과를 보기 전에 기록한다.** 결과를 본 뒤 기준을 바꾸면 사전 등록이 무효가 된다.

**주목**: G2·G3가 실패해도 프로젝트가 성립한다. 차용 설계의 가장 큰 이점이 여기 있다 — **"선행 표적 목록이 검증되지 않는다" 또는 "약이 될 수 없다"가 그 자체로 발표 가능한 결과**다.

---

## 11. 일정 (14일 · 2인)

| Day | 담당 A (생물정보) | 담당 B (화학정보) |
| --- | --- | --- |
| 0 | **사전 등록 커밋**: 대조군 세트·판정 기준·게이트 완화 규칙 | 환경 점검 (REINVENT4, RDKit, 도킹 툴) |
| 1 | **두 논문 supplementary에서 유전자 풀 추출 → G1 판정.** Ensembl ID 매핑 | Open Targets EFO id 확정, GraphQL 쿼리 작성 |
| 2 | **GSE162694 추세 검정** (보정/무보정 2모형), 차용 풀 대상 | 음성 대조군 strict 요건(표적 결합) 문헌 채우기 |
| 3 | **G2 판정.** 보정 전후 판정 변화 유전자 목록 확정 | GSE202379 세포형 마커 도출 → 후보 귀속 |
| 4 | Open Targets 근거·tractability 병합 | 경로 소속 주석 (KEGG/Reactome/Hallmark) |
| 5 | **스코어카드 초안 완성** (대조군 포함 전 행) | Pathway Commons 네트워크 거리 |
| 6 | **G3 판정. 표적 확정.** | 표적 PDB·공개 리간드 수집 |
| 7 | Geneformer 세포형 패널 + 문헌 일치 패널 | 도킹 프로토콜 구축 |
| 8 | 민감도 분석 정리 (보정 유무, leave-one-out) | **도킹 검증 (활성 vs 비활성)** |
| 9 | 스코어카드 최종본, 그림화 | **G4 판정. REINVENT4 본실행** |
| 10 | 고찰 초안 (§15 6항목) | 생성 분자 필터링·물성 계산 |
| 11 | 포스터 레이아웃 | 물성 산점도·scaffold 신규성 |
| 12 | 포스터 제작 | 포스터 제작 |
| 13 | 포스터 완성·인쇄 발주 | 발표 원고 |
| 14 | **리허설 ≥ 5회** | |

**10일 압축안**: Day 4↔5 병합, Day 7 Geneformer 패널을 발현 수준 확인으로 축소, Day 8 민감도 분석을 보정 유무 1종으로 축소. **G1–G4와 사전 등록은 어떤 경우에도 생략하지 않는다.**

---

## 12. 방법론상 확정된 제약 (되돌리지 않음)

**R1 — Geneformer in silico perturbation을 표적 선정 근거로 쓰지 않는다.**
- *Adversarial validation of in silico perturbation profiles* (Comput Biol Chem, PMID 42033879): 21개 유전자, 발현량 매칭 대조 61개, **0/21이 FDR 통과 실패**. 저자 결론: raw embedding shift는 발현 수준에 substantially confounded. 유전자 집합 수준 Wilcoxon p=0.051
- *Evaluating Foundation Models for In-Silico Perturbation* (scISP, bioRxiv 2025.05.11.653338): zero-shot ISP 정확도 **Geneformer 14.73% / MRR 0.0763**, scGPT 14.28%. 단 **cell state separation은 96/224 통과** → 임베딩·분류는 쓸 만하고 perturbation 추론이 약하다

**R2 — 신규 표적 주장을 "선행 연구가 없음"으로 세우지 않는다.** 신규성 주장은 §9.2-6의 scaffold 거리 정량으로 대체한다.

**R3 — 리간드가 없는 표적에 REINVENT4를 돌리지 않는다.** §5.3의 SM_B4 하한이 이 문제를 구조적으로 차단한다.

**R4 — 코호트 간 padj·DEG 개수를 나란히 비교하지 않는다.** 원본 주석 규모와 전처리가 다르면 다중검정 부담이 근본적으로 다르다 (GSE135251 원본 64,253 vs GSE162694 31,683).

**R5 — 차용 사실을 숨기지 않는다.** §3 표를 포스터 방법 섹션 첫 줄에 배치한다.

---

## 13. 위험과 대응

| # | 위험 | 대응 |
| --- | --- | --- |
| A | Yang et al. 26개 목록 확보 실패 → 풀이 15개 | G1의 완화 규칙(SM_B6까지). 15개여도 스코어카드는 완결되므로 프로젝트 성립 |
| B | 차용 풀이 GSE162694에서 재현되지 않음 | **결과로 보고** (G2). §2.2 문제의식이 강화됨 |
| C | SM_B1–B4 후보가 0개 | **결과로 보고** (G3). REINVENT4는 P2 대조군 대상으로 실행 |
| D | "논문에서 가져왔을 뿐"이라는 지적 | §3 표 + §5.1 독립 검증 + §5.3 약물성 게이트가 답. **먼저 공개하면 설계의 일부가 되고, 숨기면 약점이 된다** |
| E | 최종 표적이 이미 개발 중 | 정면 인정 + §9.2-6 scaffold 거리로 차별화 |
| F | GSE162694 F3(8명)·F4 생검(4명)이 적음 | 추세 모형이 104명 전체를 사용. 상위 병기 기여도를 leave-one-out으로 확인해 보고 |
| G | `MAOA`가 표적이 될 경우 "CNS 약물인데 간에?" | 오히려 고찰 재료: 간 선택적 분포 설계, MAO-A/B 선택성, 기존 MAOI의 티라민 상호작용 회피 — 전부 약학 논점 |

---

## 14. 선행 연구와의 관계

**두 논문은 경쟁자가 아니라 입력이다.** 포스터에 이렇게 쓴다:

| | Li et al. (2026) | Yang et al. (2026) | **본 연구** |
| --- | --- | --- | --- |
| 목적 | 표적 유전자 발굴 | 병인 공통 시그니처 | **제안된 표적이 약이 될 수 있는가** |
| 공변량 | 명시 없음 | 체계적 미사용 | **연령·성별 보정** |
| 검증 코호트 | GSE14323·GSE84044 (자기 데이터) | 4개 코호트 (자기 데이터) | **GSE162694 — 양쪽 모두 미사용** |
| 약물성 평가 | **없음** | **없음** | **Open Targets tractability 게이트** |
| 세포형 귀속 | 없음 (LX-2 세포주만) | 없음 | **환자 조직 snRNA 귀속** |
| 종착점 | 바이오마커 / AQP1 | hub gene | **소분자 선도 물질** |

**두 논문의 헤드라인 유전자 교집합이 ∅라는 사실**(§2.2)도 포스터에 넣는다. 우리가 약물성이라는 **다른 축**을 도입하는 이유가 된다.

---

## 15. 포스터 / 발표

### 7분 발표

| 시간 | 내용 |
| --- | --- |
| 0:00–1:00 | MASH에 2024·2025년 처음으로 약이 나왔다 |
| 1:00–1:50 | **그런데 섬유화는 4명 중 1명만 좋아진다.** 두 약 모두 대사 축이고, 실제로 승인약 표적은 섬유화 병기와 함께 변하지 않는다 |
| 1:50–2:50 | **표적 목록은 이미 있다. 그런데 2026년 두 논문의 헤드라인 유전자가 하나도 안 겹치고, 아무도 약이 될 수 있는지는 묻지 않았다** |
| 2:50–4:20 | 우리가 한 것: 독립 코호트 검증(연령·성별 보정) → 세포형 귀속 → 약물성 게이트 |
| 4:20–5:00 | **스코어카드: 대부분이 소분자로 접근 불가. AQP1은 근거가 가장 강한데 약물성이 가장 낮다** |
| 5:00–6:30 | **살아남은 표적에 대해 실제로 설계한 분자와 물성 지형** |
| 6:30–7:00 | 결론 + 한계 |

### A0 포스터 8패널

① §2.1 미충족 수요 막대 + 승인약 표적 비-DEG 사실
② §2.2 두 논문 벤다이어그램 (교집합 ∅)
③ 차용 vs 우리 것 표 (§3) + 검증 코호트 QC (§4.2 깊이·복잡도)
④ 보정 전후 판정 변화 유전자
⑤ 세포형 귀속
⑥ **표적 약물성 스코어카드** ← 가장 크게
⑦ 대조군 통과 궤적 (§6.2)
⑧ **생성 분자 물성 산점도 + scaffold 신규성**

### 고찰 6항목 (전부 약학)

1. **표적 유효성 ≠ 약물성** — AQP1 사례 (§5.5)
2. **분비 리간드는 소분자 표적이 아니다** — 케모카인 4종을 수용체(CXCR3 등)로 옮겨야 하는 이유
3. **구조 단백질·담도 표지자는 발현 순위가 항상 올린다** — LUM, EPCAM과 전 프로젝트 음성 대조군의 동질성
4. **공변량 보정이 판정을 바꾼다** — 선행 논문이 보정하지 않아 생긴 차이
5. **표적 세포형이 약물 설계 요건을 정한다** — HSC 도달성, 간 선택적 분포
6. **한계** — in vitro 활성 검증 부재, 도킹 점수는 결합친화도가 아님, 선택성 미평가, 차용 목록 자체의 방법 의존성

---

## 16. 개정 이력

| 버전 | 일자 | 내용 |
| --- | --- | --- |
| v1.0 | 2026-09-18 | `/fibrosis/` F4 특이 표적 프로젝트 폐기에 따른 신규 계획. 병인 무관 간섬유화로 전환, 병기를 순서형 추세로 전환, 연령·성별 보정을 코호트 선정 기준으로 승격, 세포조성 교란 처리 추가 |
| **v2.0** | **2026-09-18** | **자체 전장 DEG 파이프라인 폐기 → 선행 논문 2편의 표적 목록 차용.** 연구 질문을 "섬유화 표적은 무엇인가"에서 **"제안된 표적이 약이 될 수 있는가"** 로 전환. 주 산출물을 퍼널 → **표적 약물성 스코어카드**로 변경. GSE162694를 양 논문 미사용 독립 검증 코호트로 지정. GSE84044·GSE135251·GSE14323·GSE197112는 차용 논문의 입력이므로 우리 검증에 사용하지 않음. 일정 4일 단축분을 REINVENT4·도킹 검증·포스터에 재배분 |
